# StockQuant Product Specification

> **项目名称**：StockQuant 2.0 — 机构级中国 A 股量化交易平台
> **版本**：2.0.0（系统设置页设计版 v2.0.3）
> **创建日期**：2026-06-14
> **更新日期**：2026-06-14（项目经理复核，修复 14 个问题）
> **状态**：Ready for Development

***

## 1. 产品概述

### 1.1 产品定位

StockQuant 2.0 是一个面向**专业量化开发者**的**AI 原生机构级**中国 A 股量化交易平台，聚焦**中国 A 股市场**。

**核心定位**：通过 AI 的强大信息收集与推理能力，实现从数据采集 → 指标计算 → 策略配置 → 回测验证 → 实时盯盘 → AI 辅助决策 → 风控执行的全流程闭环。

**从"入门学习工具"升级到"AI 驱动的机构级平台"**，不仅对标 vnpy、backtrader、vectorbt 的机构级能力，更通过 AI Agent 架构实现量化交易的智能化闭环。

### 1.2 目标用户

| 角色         | 描述              | 核心诉求                  |
| ---------- | --------------- | --------------------- |
| 资深个人量化开发者  | 有多年实盘经验，从入门工具进阶 | 策略框架成熟、执行稳定、数据可靠、上手快速 |
| 专业量化研究员    | 在私募/券商自营/基金工作   | 回测可信、多策略并行、风控合规       |
| 金融工程学生/从业者 | 学术研究+实战         | 接口清晰、文档完善、指标全面        |

> **渐进式平台定位**：StockQuant 2.0 既支持入门用户通过自然语言快速开始（5 分钟第一个回测），也支持专业用户通过完整 API 构建机构级策略。简单场景简单用，复杂场景深度用。

### 1.3 产品愿景

> 通过 AI 的信息收集、降噪、推理能力，构建覆盖量化交易全流程的闭环平台，让每一个量化交易者都能拥有机构级别的回测、风控、执行和 AI 决策辅助能力。

### 1.4 全流程闭环设计

StockQuant 2.0 以 AI 为中枢，覆盖量化交易全流程：

```
数据采集 → 指标计算 → 策略配置 → 回测验证 → 实时盯盘 → AI 辅助决策 → 风控执行
    ↑                                                                            │
    └──────────────────── 反馈闭环 ───────────────────────────────────────────────┘
```

| 阶段   | 传统量化      | StockQuant 2.0 AI 增强                |
| ---- | --------- | ----------------------------------- |
| 数据采集 | 被动获取结构化数据 | AI Agent 主动爬取新闻、公告、研报、社交媒体，结构化存入数据湖 |
| 指标计算 | 手工编写技术指标  | AI 自动推荐最优指标组合，生成并回测候选指标             |
| 策略配置 | 手动编写策略代码  | AI 对话式生成策略、自然语言描述策略意图、自动参数调优        |
| 回测验证 | 回测后看报表    | AI 自动解读回测结果，指出问题，给出优化建议             |
| 实时盯盘 | 手动设置条件单   | AI 实时分析行情 + 新闻 + 资金流，主动推送机会/风险      |
| 辅助决策 | 依赖技术指标    | AI 综合技术面 + 基本面 + 消息面，生成交易信号建议       |
| 风控执行 | 硬编码规则     | AI 动态调整风控参数，根据市场环境自适应               |

***

## 2. 核心设计理念

### 2.1 AI 原生架构

StockQuant 2.0 不是简单的"量化框架 + AI 插件"，而是以 AI Agent 为中枢的**原生架构**。

**AI Agent 架构**：

```
┌─────────────────────────────────────────────────┐
│              AI Agent Orchestrator               │
│  (策略编排 + 信号融合 + 决策推荐 + 风险审核)      │
├─────┬──────┬───────┬───────┬───────┬────────────┤
│Data │Indic │Strat  │Backtest│Monitor│Risk/Exec   │
│Agent│ator  │Agent  │Agent   │Agent  │Agent       │
└─────┴──────┴───────┴───────┴───────┴────────────┘
```

- **DataAgent**：多源数据采集、清洗、去噪、结构化、特征工程
- **IndicatorAgent**：自动发现有效指标、指标组合优化
- **StrategyAgent**：自然语言生成策略、参数自动优化、策略评分
- **BacktestAgent**：自动解读回测结果、发现过拟合、提出改进
- **MonitorAgent**：实时行情 + 消息面监控、异常检测、机会预警
- **RiskAgent**：动态风控参数调整、市场环境评估、异常交易拦截

**Agent 间通信协议**：

- 所有 AI Agent 通过统一的 `AIEvent` 事件总线通信（复用 `EventEngine`，扩展事件类型）
- `AIEvent` 类型：`NewsEvent`（新闻到达）、`SentimentEvent`（情绪突变）、`SignalEvent`（交易信号建议）、`AlertEvent`（告警）、`MemoryUpdateEvent`（记忆更新）
- 通信模式：
  - **异步事件驱动**：DataAgent 采集 → 发布 NewsEvent → MonitorAgent/DecisionAgent 消费
  - **同步请求/响应**：BacktestAgent 请求 DecisionAgent 解读（等待响应）
  - **共享内存**：MemorySystem 作为所有 Agent 的共享状态（L1/L2/L3）
- DataAgent 采集完成后发布 `NewsEvent`，MonitorAgent 和 DecisionAgent 作为订阅方消费该事件
- LLM 调用采用异步 `httpx`，不阻塞事件总线

### 2.2 与 v1 的根本差异

| 维度      | v1（现状）                 | v2（目标）                                               |
| ------- | ---------------------- | ---------------------------------------------------- |
| 回测引擎    | 手动逐 Bar 循环             | 事件驱动引擎（EventEngine + EventQueue）                     |
| 订单管理    | 直接写 CSV                | 完整 OMS，5 种订单类型，生命周期管理                                |
| 投资组合    | 单标的、单方向                | 多资产、多空双向、仓位聚合                                        |
| 策略抽象    | 无基类，自由写类               | BaseStrategy + 生命周期钩子 + AI 策略生成                      |
| 回测指标    | 4 个（收益/回撤/胜率/利润）       | 30+ 指标 + AI 自动解读                                     |
| 佣金/滑点   | 零建模                    | 完整 A 股费用模型（印花税 0.05% + 佣金 + 过户费 + 滑点）                |
| 实盘交易    | easytrader 同花顺 GUI 自动化 | 券商官方 API（中泰 XTP / CTP） + 模拟盘                         |
| 数据层     | 硬编码多个数据源               | DataFeed 抽象接口 + 本地缓存 + AI 数据采集                       |
| 可视化     | matplotlib 简单曲线        | Plotly 交互式 + HTML 报表 + Streamlit Dashboard + AI 图表解读 |
| AI 能力   | 无                      | AI Agent 全流程覆盖（采集/指标/策略/回测/监控/风控）                    |
| 测试      | 无                      | pytest 全覆盖 + CI/CD                                   |
| 部署      | pip install            | Docker + Helm                                        |
| AI 记忆系统 | 无                      | 三级记忆架构（L1/L2/L3）                                     |
| AI 反幻觉  | 无                      | 五步纠正 + 幻觉数据库                                         |
| AI 数据存储 | 无                      | SQLite/PostgreSQL + ChromaDB 向量库                     |
| AI 配置机制 | 无                      | YAML 配置驱动                                            |

***

## 3. 功能需求

### F001 事件驱动回测引擎（P0）

**用户场景**：量化研究员编写策略后，通过一条命令运行回测，引擎自动按时间顺序派发 Bar 事件，策略响应后生成订单，引擎模拟成交并记录持仓变化。

**功能描述**：

- 实现 `EventEngine` 事件调度器，支持同步/异步事件派发
- 事件类型：`TimerEvent`、`BarEvent`、`TickEvent`、`TradeEvent`、`OrderEvent`、`AccountEvent`、`PortfolioEvent`
- 实现 `Cerebro` 主引擎，支持 `add_data()` / `add_strategy()` / `add_analyzer()` / `run()` 流畅 API
- 支持多时间框架（如同时加载 1min 和 1d 数据）
- 回测进度条 + 进度百分比实时显示

**验收标准**：

- 回测速度：日线 10 年数据，5 只股票，单核 ≥ 5000 Bar/秒
- 支持 1min 数据回测，10 年数据量 ≥ 500 Bar/秒
- 事件派发零丢失（通过集成测试验证）
- 多标的回测正确聚合投资组合盈亏

### F002 订单管理系统 OMS（P0）

**用户场景**：策略发出买入指令后，订单进入 OMS 管理，经历"提交 → 排队 → 部分成交 → 全部成交"的完整生命周期。支持限价单、市价单、止损单、止损限价单。

**功能描述**：

- `Order` 数据类：`order_id`、`symbol`、`side`（Buy/Sell/Short/Cover）、`type`（Market/Limit/Stop/StopLimit）、`price`、`quantity`、`filled_quantity`、`filled_price`、`status`、`timestamps`
- 订单状态机：PENDING → SUBMITTED → QUEUED → PARTIAL → FILLED / CANCELLED / REJECTED
- 5 种订单类型完整支持
- 支持 A 股 T+1 规则（当日买入不可当日卖出）
- 支持 100 股整数倍规则
- 支持涨跌停板限制（主板 10%、创业板/科创板 20%）

**验收标准**：

- 限价单按价格优先、时间优先原则撮合
- 市价单按当前 Bar 收盘价撮合（可配置）
- 止损单在触发后自动转限价单
- 涨停板买单、跌停板卖单被正确拒绝
- T+1 规则：当日买入持仓不可在当日卖出

### F003 投资组合模拟（P0）

**用户场景**：用户同时持有 10 只股票，投资组合跟踪每只股票的持仓数量、成本价、市值、盈亏，以及总现金、总权益、权益曲线。

**功能描述**：

- `Portfolio` 类管理全局资金和所有持仓
- 持仓信息：数量、成本价、市值、未实现盈亏、已实现盈亏
- 账户信息：初始资金、可用现金、冻结资金、总权益
- 支持保证金交易（融资融券模拟，可配置杠杆率）
- 权益曲线逐 Bar 更新（含手续费、滑点）
- 支持多策略共享同一个 Portfolio

**验收标准**：

- 单笔买入 100 股，价格 50 元，佣金 0.025%，过户费 0.001%，冻结资金 = 50 × 100 × (1 + 0.025% + 0.001%) + 0.001%
- 全部卖出后权益变化与手工计算一致（误差 < 0.01 元）
- 多策略并发下单不出现资金超买

### F004 策略框架（P0）

**用户场景**：开发者通过继承 `BaseStrategy` 编写策略，实现 `on_start`、`on_bar`、`on_tick`、`on_order`、`on_trade`、`on_finish` 钩子函数。框架自动调用。

**功能描述**：

- `BaseStrategy` 抽象基类，定义完整生命周期钩子
- 内置指标计算代理：`self.EMA()`、`self.MACD()`、`self.BOLL()` 等，返回值支持 `__getitem__` 访问历史值
- 指标交叉检测：`self.ma_short.crossed_above(self.ma_long)`
- 订单发送：`self.order_market(price, quantity)`、`self.order_limit(price, quantity)`
- 仓位管理：`self.close_all()`、`self.get_position()`、`self.set_position(quantity)`
- 参数声明：类级 `parameters` 字典，支持参数优化
- 日志：`self.log()` 记录策略运行日志

**验收标准**：

- MA 交叉策略：EMA(5) 上穿 EMA(20) 买入，下穿卖出，回测结果与手工计算一致
- 参数优化时策略自动以不同参数运行
- 策略生命周期钩子按正确顺序调用
- 指标计算结果与 TA-Lib 独立计算结果一致（误差 < 1e-6）

### F005 回测统计指标（P0）

**用户场景**：回测结束后，一键生成包含 30+ 指标的完整报告，包括收益、风险、风险调整后收益、交易统计等。

**功能描述**：

**收益类指标**：

- 累计收益率、年化收益率、超额收益率（相对基准）

**风险类指标**：

- 最大回撤、最大回撤持续时间、平均回撤、回撤恢复时间

**风险调整后收益**：

- Sharpe Ratio（无风险利率可配置，默认 2.5% 存款利率）
- Sortino Ratio
- Calmar Ratio
- Omega Ratio
- Information Ratio
- Treynor Ratio

**交易统计**：

- 总交易次数、胜率、平均盈利/亏损、盈亏比
- 最长连盈/连亏次数
- 平均每笔收益、总手续费、总滑点

**其他指标**：

- SQN（System Quality Number）
- Kelly %
- 月度/年度收益表
- Beta、Alpha（相对沪深 300 基准）
- VaR (95%)、CVaR (95%)
- 波动率（日/周/年）

**验收标准**：

- Sharpe Ratio 计算结果与 numpy/pandas 手工计算一致（误差 < 1e-4）
- 报告包含所有 30+ 指标
- 支持自定义基准（默认沪深 300）

### F006 Broker 抽象层（P0）

**用户场景**：同一套策略代码，通过切换 Broker 实现回测/模拟盘/实盘三种模式，无需修改策略逻辑。

**功能描述**：

- `Broker` ABC 基类定义统一接口
- `BacktestBroker`：回测模式，基于 K 线数据模拟成交
- `PaperBroker`：模拟盘模式，基于实时行情模拟成交（含滑点，不实际下单）
- `LiveBroker`：实盘模式，调用券商 API 实际下单
- 三种 Broker 共享相同接口：`place_order()`、`cancel_order()`、`get_positions()`、`get_balance()`、`get_history()`

**验收标准**：

- 同一策略在 BacktestBroker 和 PaperBroker 下生成的交易记录一致（允许滑点差异）
- 切换 Broker 只需修改一行配置代码

### F007 佣金与滑点建模（P0）

**用户场景**：回测中每笔交易自动扣除 A 股市场真实费用，确保回测结果贴近实际。

**功能描述**：

- `CommissionInfo` 数据类：
  - 买入佣金：0.025%（最低 5 元）
  - 卖出佣金：0.025%（最低 5 元）
  - 印花税：0.05%（卖方，政策变动可配置）
  - 过户费：0.001%
- `SlippageModel` 抽象类：
  - `FixedSlippage`：固定滑点（如 0.01 元）
  - `PercentSlippage`：百分比滑点（如 0.1%）
  - `AdaptiveSlippage`：基于成交量自适应滑点
- 费用在成交时自动扣除

**验收标准**：

- 买入 10000 元股票，扣除后实际成交 = 10000 × (1 + 0.025% + 0.001%)
- 卖出 10000 元股票，扣除后实际收入 = 10000 × (1 - 0.025% - 0.05% - 0.001%)
- 滑点模型在回测中正确应用

### F008 参数优化器（P1）

**用户场景**：用户声明策略参数范围，引擎自动并行运行所有参数组合，输出排名报表。

**功能描述**：

- `Cerebro.optstrategy()` 方法
- 支持网格搜索（Grid Search）和随机搜索（Random Search）
- 多线程/多进程并行优化（默认 CPU 核心数）
- 优化目标可配置：Sharpe Ratio、总收益、最大回撤等
- 输出参数排名表（Top 20）
- 支持 Walk-Forward 分析（滚动窗口优化）

**验收标准**：

- 100 组参数在 4 核机器上的优化时间 < 单组时间的 4 倍（证明并行有效）
- 输出结果包含所有参数组合的完整指标
- Walk-Forward 正确划分训练/测试窗口

### F009 风险管理模块（P1）

**用户场景**：引擎在策略订单到达 Broker 之前，自动检查是否违反风控规则，拦截不合规订单。

**功能描述**：

- `RiskManager` 拦截器模式
- 可配置风控规则：
  - 单只股票最大仓位（占总资产 30%）
  - 单只股票最大买入金额
  - 总仓位上限（占总资产 90%）
  - 单日最大亏损（2%）
  - 累计最大回撤熔断（15%）
  - 订单频率限制（1 分钟最多 10 笔）
  - 涨跌停板过滤
- 风控违规自动记录日志并拒绝订单
- 支持全局熔断：市场单日跌幅超过 5% 暂停所有交易

**验收标准**：

- 违反任一风控规则时订单被拒绝，日志记录原因
- 累计回撤达到 15% 后策略继续运行但不再下单
- 风控检查耗时 < 1ms/订单

### F010 仓位管理模块（P1）

**用户场景**：策略通过仓位管理器自动计算每笔交易的买入数量，而不是手动指定固定手数。

**功能描述**：

- `PositionSizer` 抽象类 + 多种实现：
  - `FixedFractionSizer`：固定比例（如每次投入总资金的 10%）
  - `KellySizer`：凯利公式计算
  - `ATRSizer`：基于 ATR 的波动率仓位
  - `VolatilityTargetSizer`：波动率目标仓位（年化目标 20%）
  - `EqualWeightSizer`：等权重分配
- 支持组合使用（如：Kelly 计算基础仓位 × ATR 调整）

**验收标准**：

- 各仓位管理算法计算结果与手工计算一致
- 仓位大小受风控模块限制（不超限）

### F011 数据层抽象（P1）

**用户场景**：用户通过统一的 `DataFeed` 接口获取任意数据源的数据，切换数据源不需要修改策略代码。

**功能描述**：

- `DataFeed` ABC 基类：
  - `get_kline(symbol, timeframe, start_date, end_date, adj)` → DataFrame
  - `get_tick(symbol)` → Tick 数据类
  - `get_fundamental(symbol, metric, period)` → DataFrame
- 内置数据源实现：
  - `BaoStockFeed`：历史 K 线（主力）
  - `TuShareFeed`：基本面 + 新股 + 股票列表
  - `NetEaseFeed`：实时 Tick
  - `CSVFeed`：本地 CSV 文件
  - `ParquetFeed`：本地 Parquet 文件（高性能）
  - `SQLiteFeed`：SQLite 数据库
  - `WebSocketFeed`：WebSocket 实时数据流（预留接口）
- `DataCache` 层：
  - 首次请求时从数据源下载并缓存
  - 增量更新（只拉取新增数据）
  - 缓存格式：Parquet（列式，高性能）
  - 本地缓存路径可配置

**验收标准**：

- 缓存命中时数据读取速度 > 直接 API 调用 10 倍
- 切换数据源只需修改 `Cerebro.adddata(DataFeed("BaoStock", ...))`
- Parquet 缓存写入 ≤ 500ms/千行

### F012 模拟盘模式（P1）

**用户场景**：回测通过后，用户可以一键切换到模拟盘，使用真实行情验证策略，不花一分钱但体验真实交易环境。

**功能描述**：

- `PaperBroker` 使用实时行情模拟成交
- 与 `BacktestBroker` 相同的费用模型和撮合逻辑
- 实时持仓和权益跟踪
- 钉钉/邮件实时推送模拟交易通知
- 模拟盘成绩与回测结果对比

**验收标准**：

- 模拟盘与回测结果误差 < 1%（允许实时数据与回测数据微小差异）
- 模拟盘期间策略正常运行 ≥ 24 小时无崩溃

### F013 回测报表系统（P1）

**用户场景**：回测结束后自动生成包含图表和指标的 HTML 报表，可以邮件发送给团队审阅。

**功能描述**：

- `ReportGenerator` 模块：
  - 指标汇总表（30+ 指标表格）
  - 资金曲线图（Plotly 交互）
  - 回撤曲线图
  - 月度收益热力图
  - 交易明细表（可分页）
  - 参数优化排名表
- 输出格式：
  - HTML（默认，含 Plotly 交互式图表）
  - PDF（可选，通过 weasyprint 转换）
  - JSON（机器可读，供外部系统消费）
- 支持自定义报表模板（Jinja2）

**验收标准**：

- HTML 报表包含所有图表和指标
- PDF 报表排版美观，支持中文
- 报表生成时间 < 5 秒（1000 笔交易以内）

### F014 内置策略模板库（P2）

**功能描述**：

- `MA_Crossover_Strategy`：均线交叉策略
- `Bollinger_Band_Strategy`：布林带突破策略
- `MACD_Divergence_Strategy`：MACD 背离策略
- `RSI_Reversal_Strategy`：RSI 超买超卖反转策略
- `Dual_Thrust_Strategy`：Dual Thrust 突破策略
- `Mean_Reversion_Strategy`：均值回归策略
- `Momentum_Strategy`：动量策略
- 每套模板附带使用文档和回测示例

### F015 自定义指标 DSL（P1）

**功能描述**：

- `@indicator` 装饰器：将普通函数转换为指标对象
- `Indicator` 基类：支持自定义指标继承
- 指标链式调用：`self.EMA(self.RSI(14))`
- 指标可视化：`self.plot_indicator(indicator, name="MyIndicator")`

### F016 Web Dashboard（P2）

**功能描述**：

- 基于 Streamlit 的 Web 应用
- 页面：
  - 数据浏览器：查看和下载历史数据
  - 策略编辑器：在线编写和编辑策略
  - 回测配置：选择数据、参数、优化设置
  - 结果仪表盘：资金曲线、指标表、交易明细
  - 参数优化看板：参数扫描散点图
  - 模拟盘监控：实时持仓和通知
- 支持多用户（未来）

### F017 券商 API 实盘交易（P2）

**功能描述**：

- 替代 easytrader 的同花顺 GUI 自动化
- 集成中泰 XTP API（A 股）
- 集成 CTP API（期货）
- `LiveBroker` 实现真实下单
- 订单审计日志（每笔下单记录原因、时间、价格、数量）
- 合规性检查（符合券商用户协议）

**验收标准**：

- 通过券商 API 成功下单并成交（模拟账户验证）
- 订单审计日志完整记录所有操作
- 实盘和模拟盘使用相同策略代码

### F018 消息推送系统（P1）

**功能描述**：

- 钉钉推送（现有，保留）
- 邮件推送（现有，保留）
- 新增企业微信推送
- 新增 Telegram 推送
- 推送事件可配置：
  - 交易通知（买入/卖出/成交/撤单）
  - 风控告警（触发风控规则）
  - 系统告警（引擎异常、数据中断）
  - 回测完成通知
  - 定时报告（每日收盘后推送日总结）

***

### F019 信号管线系统（P0）

> **定位**：连接 AI 信息处理全流程（F020）与策略执行框架（F004）的桥梁。AI 升华输出的是自然语言洞察，策略框架执行的是代码信号，信号管线负责两者之间的双向转换。

**功能描述**：

- **AI 信号 → 策略信号转换**：
  - 将 F020 升华输出的交易建议转换为策略可执行的参数化信号（如`signal = Signal(symbol="sh600519", side=BUY, confidence=0.75, source="ai_elevation")`）
  - 每个 AI 信号必须附带 `reasoning`（推理链引用）和 `confidence`（置信度评分）
  - 信号过期机制：AI 建议有有效期（如新闻建议 30 分钟过期，公告建议 4 小时过期）
- **策略信号 → AI 反馈**：
  - 策略执行的信号反向记录到 AI 记忆系统，供 F020 升华参考
  - AI 可对比"自己的建议"与"策略实际执行的信号"，计算一致性，持续优化
- **信号冲突解决**：
  - 当 AI 信号与传统策略信号矛盾时，触发 F025 决策 Agent 进行仲裁
  - 仲裁规则可配置：默认"保守模式"（AI 否决时暂停，AI 建议时仍需策略确认）
- **信号优先级矩阵**：

| 信号来源      | 优先级 | 是否需要人工确认   | 可自动执行  |
| --------- | --- | ---------- | ------ |
| 传统策略信号    | P0  | 否          | 是      |
| F024 盯盘信号 | P1  | 否（推送）      | 否      |
| F025 决策信号 | P1  | 是（半自动/全自动） | 是（需授权） |
| AI 升华建议   | P2  | 是          | 否      |

- **A 股规则约束**（所有信号必须满足）：
  - 数量必须是 100 的整数倍
  - 买入建议需知会 T+1 限制（当日买入不可当日卖出）
  - 建议价格必须在涨跌停范围内（主板 ±10%，创业板/科创板 ±20%）
  - 建议需包含预估费用（佣金 0.025% + 印花税 0.05% 卖方 + 过户费 0.001%）

**验收标准**：

- AI 信号到策略信号的转换延迟 < 100ms
- 信号冲突解决成功率 ≥ 90%
- 所有 AI 信号 100% 通过 A 股规则校验
- 信号过期自动失效，不会触发过期交易

***

### F020 AI 信息处理全流程（P0）

**用户场景**：AI 不是孤立的"采集+决策"两阶段工具，而是构建了一个从信息采集 → 信息降噪 → 信息总结 → 信息升华的完整信息处理链。记忆系统和反幻觉机制深度嵌入到**每一个环节**，确保信息从进入系统到驱动决策的全生命周期都准确、可靠、可追溯。

**核心设计**：信息处理全流程 + 记忆系统 + 反幻觉系统三位一体，记忆与反幻觉不是外挂模块，而是每个环节的内建能力。

***

#### 信息处理全流程架构

```
原始数据流
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│ 环节1: 信息采集 (Information Collection)                 │
│                                                         │
│ 多源并行采集 → 反幻觉(来源验证) → 记忆(写入L2) → 去重   │
│                                                         │
│ • 采集即验证：每条新数据入库前，与记忆中的历史数据对比    │
│   检测事实矛盾 → 触发反幻觉 → 标记置信度                │
│ • 采集即记忆：所有原始数据和初步分析自动写入 L2           │
│ • 采集即降噪：与记忆对比，过滤已知重复/低信源内容         │
│                                                         │
└─────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│ 环节2: 信息降噪 (Information Denoising)                  │
│                                                         │
│ 去重 → 去噪 → 降权 → 过滤 → 反幻觉(一致性检查)          │
│                                                         │
│ • 记忆辅助降噪：用 L3 长期记忆中的"噪音模式库"过滤        │
│   已知低价值内容（如论坛灌水、营销号、标题党）            │
│ • 反幻觉辅助降噪：对比记忆中的已验证事实，自动过滤        │
│   与已证伪信息相似的内容（传播链阻断）                    │
│ • 时效性过滤：基于记忆中的信息有效期，自动降权过期信息    │
│ • 信源可信度排序：结合 L3 中各信源的准确度历史，动态      │
│   降权低可信度信源的内容                                 │
│ • 冗余压缩：相似信息自动合并，保留最高置信度的一条        │
│                                                         │
└─────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│ 环节3: 信息总结 (Information Summarization)              │
│                                                         │
│ 上下文检索(L1/L2/L3) → 结构化 → LLM 总结 →              │
│ 反幻觉(五步验证) → 记忆(写入L2/L3)                       │
│                                                         │
│ • 总结前检索记忆：先检索 L1 工作记忆 + L2 短期记忆        │
│   中相关的上下文，确保总结基于完整信息集                   │
│ • 总结中注入反幻觉：在 LLM Prompt 中附加"事实清单"        │
│   和"已验证信息"，限制 LLM 自由发挥的空间                 │
│ • 总结后反幻觉：总结输出经过事实验证 + 来源验证，          │
│   确保总结中每个数据点都有可靠来源                         │
│ • 总结即记忆：验证后的总结存入 L2 作为"预摘要"，           │
│   重大总结（日/周/月报）自动升级写入 L3                   │
│ • 用户参与：用户可编辑总结，修正后的内容进入 L3            │
│                                                         │
└─────────────────────────────────────────────────────────┘
  │
  ▼
┌─────────────────────────────────────────────────────────┐
│ 环节4: 信息升华 (Information Elevation)                  │
│                                                         │
│ 检索记忆(L3) + 当前上下文 → 多源融合 → 推理               │
│ → 反幻觉(交叉验证) → 记忆(写入L3) → 输出建议             │
│                                                         │
│ • 升华前提：从 L3 长期记忆中检索已验证的市场规律、         │
│   有效策略逻辑、高置信度事件分析作为推理基础              │
│ • 升华过程：将分散在 L2/L3 中的碎片信息（新闻/情绪/        │
│   技术信号/基本面）融合为结构化洞察（如"白酒板块即将      │
│   迎来政策催化"）                                        │
│ • 升华中反幻觉：关键推理步骤必须引用记忆中的来源，          │
│   交叉验证（第二 AI 独立评估）防止推理幻觉                 │
│ • 升华即记忆：经过验证的洞察（如"政策催化+技术突破         │
│   共振"的判断）写入 L3 长期记忆，成为新的市场知识         │
│ • 升华输出：形成可执行的交易建议或策略参数建议             │
│                                                         │
└─────────────────────────────────────────────────────────┘
  │
  ▼
输出决策建议 → 反幻觉终审 → 推送用户 / 自动执行
```

***

#### 20.1 环节1 — 信息采集（记忆+反幻觉内建）

**采集流程**：

1. **多源采集**：Web Scraping + RSS + API 并行采集（东方财富、雪球、财联社、巨潮资讯、上交所等）
2. **反幻觉 — 来源验证（采集时）**：
   - 新采集的内容与记忆中的已验证数据源对比
   - 检测仿冒源（如伪造的"东方财富分析"链接）
   - 检查 URL 可达性、内容匹配度
   - 未知来源标记为"低置信度"，进入人工审核队列
3. **反幻觉 — 事实初筛（采集时）**：
   - 将采集到的关键数据（价格、PE、营收等）与记忆中的结构化数据对比
   - 发现不一致时：记录冲突 → 标记为"待核实" → 通知用户
   - 例如：采集到"茅台股价 2500 元"但记忆中缓存为 1800 元 → 标注异常，请求确认
4. **记忆 — 自动写入（采集时）**：
   - 原始数据 → L2 短期记忆（带时间戳、信源、情感打分）
   - 用户手动标记的"重要采集内容" → L3 长期记忆
5. **记忆 — 去重（采集时）**：
   - 采集内容与 L2 中已有数据做语义相似度匹配
   - 相似度 > 95% 视为重复，仅更新时间戳和阅读量
6. **记忆 — 降噪（采集时）**：
   - 与 L3 中"低信源列表"对比，来自低信源的内容自动降权
   - 与 L3 中"已知噪音模式"对比（如标题党模板、营销号模板），自动标记

**能力要求**：

- 支持配置化数据源（`data_sources.yaml`）
- 自动检测数据源变更（如页面结构调整）并告警
- 数据采集频率可配置（实时/分钟级/小时级/日级）
- 单日内采集 100+ 数据源不阻塞主引擎
- 所有采集操作自动记录到审计日志

**验收标准**：

- 新闻采集延迟 < 5 分钟（从发布到入库）
- 来源验证对已采集数据源 100% 准确
- 采集去重准确率 ≥ 99%
- 事实初筛检出率 ≥ 90%（与人工标记对比）
- 支持 ≥ 20 个数据源并行采集

***

#### 20.2 环节2 — 信息降噪（记忆+反幻觉内建）

**降噪流程**：

1. **记忆检索**：从 L3 读取信源可信度表、低价值内容模式库、信息有效期规则
2. **信源降权**：基于 L3 中信源历史准确度，动态调整各信源的权重
   - 例如：某自媒体连续 5 条信息被用户标记为错误 → 信用度降至 0.2
   - 降权后的内容在后续检索和总结中自动降权
3. **时效性降权**：基于 L2/L3 中的信息有效期规则，自动降权过期内容
   - 突发事件（公告/财报）超过 7 天 → 降权
   - 趋势性信息（行业政策）超过 30 天 → 降权
   - 结构性信息（公司基本面） → 不降权
4. **反幻觉 — 一致性过滤**：
   - 将新内容与 L2 中"已证伪信息列表"对比
   - 检测到与已证伪信息来自同一传播链的 → 自动过滤
   - 例如：某虚假消息已被证伪，AI 发现新的相似报道来自同一传播路径 → 自动过滤
5. **冗余压缩**：
   - 相似内容自动合并（如同一事件的多篇报道 → 1 条合并摘要）
   - 低置信度的重复信息被压缩丢弃，只保留最高置信度的一条
   - 合并后的摘要存入 L2，标记为"压缩摘要"

**验收标准**：

- 降噪后冗余内容保留率 ≤ 5%
- 信源降权基于历史准确度数据，非主观判断
- 时效性降权可审计（记录降权原因和依据）
- 一致性过滤检出率 ≥ 85%

***

#### 20.3 环节3 — 信息总结（记忆+反幻觉内建）

**总结流程**：

1. **记忆检索**：决策前从 L1（工作记忆）+ L2（短期记忆）+ L3（长期记忆）三源检索相关上下文
   - 语义向量搜索（Embedding）：匹配当前主题的历史经验
   - 关键词搜索：精确匹配特定标的/事件
   - 时间窗口过滤：限定检索范围（如"最近 7 天"）
   - 置信度排序：高置信度信息优先
   - 时效性排序：接近信息有效期上限的信息降权
   - Top-K 注入总结 Prompt（K 值可配置，默认 Top 10）
2. **反幻觉 — Prompt 约束注入**：
   - 在总结 Prompt 中附加"事实清单"（从记忆中检索到的已验证事实）
   - 附加"禁止编造规则"（如"只总结你检索到的信息，不要添加记忆中没有的内容"）
   - 附加"来源引用要求"（每条总结必须标注来源）
3. **LLM 生成总结**：基于检索到的记忆 + 约束后的 Prompt 生成自然语言总结
4. **反幻觉 — 总结后验证**：
   - 对总结中的每个数据点，回溯到 L2 中的原始数据进行验证
   - 事实验证：数据是否准确（如"Q3 营收同比增长 12%"是否与实际财报一致）
   - 来源验证：每条总结是否有可靠来源
   - 逻辑一致性：总结内部是否自洽
5. **记忆 — 验证后存储**：
   - 验证通过的总结 → L2 短期记忆（作为该主题的"预摘要"）
   - 重大总结（日报/周报/月报）→ L3 长期记忆
   - 用户编辑后的总结 → L3（标记为"用户修正"的高置信度）
   - 验证失败的总结 → 进入人工审核队列，记录到幻觉数据库

**总结策略**：

- **会话级总结**：每次 AI 对话开始前，自动总结近期与查询标的相关的上下文，提炼为 3-5 条关键摘要
- **日级总结**：每日收盘后自动生成"交易日报"（重要信息按标的分类 + 策略运行状态 + AI 建议与执行结果 + 市场状态变化）
- **周级/月度总结**：市场状态 + 策略表现 + 关键信息时间线 + 信息准确度回顾
- **总结压缩**：定期（默认每周）将 L2 中的原始数据压缩为 L3 摘要
  - 例如：200 条"白酒板块相关新闻" → 1 条"2024 Q4 白酒板块趋势摘要"
  - 压缩后释放 L2 存储，降低检索开销
  - 压缩前后核心事实保留率 ≥ 95%

**验收标准**：

- 总结准确率 ≥ 90%（与人工摘要对比）
- 总结后事实验证通过率 ≥ 99%
- 总结中无来源标注的比例 < 1%
- 会话级总结生成延迟 < 2 秒
- 记忆压缩后核心事实保留率 ≥ 95%

***

#### 20.4 环节4 — 信息升华（记忆+反幻觉内建）

**升华流程**：

1. **记忆检索**：从 L3 长期记忆中检索已验证的市场规律、有效策略逻辑、高置信度事件分析
2. **多源融合**：将分散在 L2/L3 中的碎片信息融合为结构化洞察
   - 技术面（K 线信号、指标突破）+ 消息面（新闻/公告）+ 情绪面（社交 sentiment）+ 基本面（财报/估值）
   - 融合为统一的结构化洞察，如：
     - "白酒板块即将迎来政策催化" = 政策信号(消息面) + RSI 底背离(技术面) + 机构增持(基本面) + 论坛看多情绪上升(情绪面)
3. **反幻觉 — 推理链验证**：
   - 每个升华结论必须附带完整的推理链（Chain-of-Thought）
   - 每步推理必须引用记忆中的来源
   - 例如："白酒板块将涨" → 因为：
     - ① 政策信号（来源：L3-2024-03-01-消费刺激政策摘要）
     - ② RSI 底背离（来源：L2-sh600519-2024-03-15 技术信号）
     - ③ 机构增持（来源：L2-基金Q1持仓报告）
     - ④ 情绪转好（来源：L2-东方财富论坛2024-03-15情绪）
   - 任一步骤无法找到记忆来源 → 结论降级为"低置信度"
4. **反幻觉 — 交叉验证**：
   - 由第二个独立的 AI（或规则引擎）重新评估同一升华结论
   - 比较两份结论的一致性
   - 不一致 → 触发人工审核
5. **记忆 — 升华结果存储**：
   - 验证通过的洞察 → L3 长期记忆（标记为"已验证洞察"）
   - 洞察与后续实际市场表现的对应关系 → L3（用于后续评估洞察质量）
   - 失败的洞察 → 记录到幻觉数据库，触发 Prompt 优化
6. **输出**：形成可执行的交易建议或策略参数建议

**验收标准**：

- 每条升华结论必须附带完整推理链（100% 有来源引用）
- 交叉验证一致率 ≥ 80%
- 升华结论与实际市场表现的对标准确率 ≥ 65%
- 无来源引用的结论标记为"低置信度"并附加警告

***

#### 20.5 全局记忆管理（贯穿全部四个环节）

记忆系统作为信息处理全流程的共享基础设施，为四个环节提供统一的记忆读写服务：

**记忆分层架构**：

| 层级      | 寿命                   | 存储                | 容量          | 内容                          |
| ------- | -------------------- | ----------------- | ----------- | --------------------------- |
| L1 工作记忆 | 当前会话（盘前→收盘）          | 内存                | \~100 条关键信息 | 当前关注标的、策略状态、最近 5 条新闻摘要、持仓状态 |
| L2 短期记忆 | 可配置（7/14/30/60/90 天） | SQLite/PostgreSQL | 百万条记录       | 原始采集数据、分析结论、交易记录、用户反馈       |
| L3 长期记忆 | 永久（除非遗忘机制触发）         | PostgreSQL        | 核心知识        | 已验证洞察、高置信度市场规律、用户手动维护知识库    |

**记忆写入规则**（每个环节的写入策略）：

| 环节 | 写入内容        | 写入层级           | 触发条件          |
| -- | ----------- | -------------- | ------------- |
| 采集 | 原始数据 + 初步分析 | L2             | 所有成功采集的数据     |
| 采集 | 低信源/噪音模式    | L3             | 用户标记或多次验证为低质量 |
| 降噪 | 冗余压缩摘要      | L2             | 相似度 > 95% 的合并 |
| 降噪 | 信源信用度变化     | L3             | 信源准确度历史变化     |
| 总结 | 会话/日/周/月总结  | L2（常规）/ L3（重大） | 总结验证通过        |
| 总结 | 用户修正内容      | L3             | 用户编辑确认        |
| 升华 | 已验证洞察       | L3             | 交叉验证通过        |
| 升华 | 失败的洞察       | 幻觉数据库          | 验证失败          |

**遗忘机制**（贯穿全部环节）：

- **时效性遗忘**：每条信息标注有效期，过期自动从 L1 → L2 → 归档。有效期由 NLP 分类器自动推断
- **置信度遗忘**：被用户标记为错误的信息自动降权，持续低于阈值的信息标记为"已证伪"
- **冗余压缩**：相似信息自动合并，低置信度重复信息被丢弃
- **记忆压缩**：定期将 L2 原始数据压缩为 L3 摘要

**记忆检索**（为每个环节提供支持）：

- 语义向量搜索（Embedding）+ 关键词搜索 + 时间窗口过滤 + 置信度排序 + 时效性排序
- 检索响应时间 < 200ms
- Top-K 注入决策 Prompt

***

#### 20.6 全局反幻觉机制（贯穿全部四个环节）

反幻觉机制作为信息处理全流程的质量守卫，在每一个环节都执行对应的验证检查：

**五类幻觉**：事实幻觉、数据幻觉、来源幻觉、推理幻觉、记忆幻觉

**各环节的反幻觉检查点**：

| 环节     | 检查点       | 检查方式               | 结果处理              |
| ------ | --------- | ------------------ | ----------------- |
| **采集** | 来源验证      | 与 L2 中已验证数据源对比 URL | 不一致 → 标记低置信度      |
| **采集** | 事实初筛      | 与 L2/L3 中缓存数据对比    | 冲突 → 标记待核实 → 通知用户 |
| **降噪** | 一致性过滤     | 与 L3 中"已证伪信息列表"对比  | 匹配 → 自动过滤         |
| **总结** | Prompt 约束 | 注入事实清单 + 禁止编造规则    | 强制 LLM 仅基于检索内容总结  |
| **总结** | 总结后验证     | 五步检测中的事实验证 + 来源验证  | 失败 → 进入人工审核       |
| **升华** | 推理链验证     | 每步推理必须有记忆来源        | 无来源 → 降级为低置信度     |
| **升华** | 交叉验证      | 第二 AI/规则引擎独立评估     | 不一致 → 触发人工审核      |
| **全部** | 置信度评分     | 综合各环节验证结果打分        | 低分 → 标记警告 / 拒绝输出  |

**五步自动化纠正流程**（在关键输出点执行）：

1. 事实验证（对比数据源）
2. 来源验证（URL 可达性 + 内容匹配）
3. 逻辑一致性检查（推理链验证 + 历史记录对比）
4. 交叉验证（第二 AI/规则引擎独立评估）
5. 置信度评分（综合打分，≥ 阈值推送 / < 阈值警告 / < 最低阈值拒绝）

**幻觉数据库**（全局积累）：

- 记录所有检测到的幻觉事件（类型、内容、AI 输出、Prompt、纠正结果）
- 定期分析幻觉模式，自动优化 Prompt
- 用户纠正后同类错误再犯率 ≤ 5%

**纠正触发模式**：

- 严格模式（默认）：所有 5 步检测必须通过
- 标准模式（只读）：全部检测，允许低置信度输出
- 宽松模式（回测/研究）：仅事实验证
- 紧急模式（连续 3 次幻觉）：强制严格模式 + 人工审核

***

#### 20.7 记忆与反幻觉的闭环反馈

```
采集 ──写入──→ 记忆 ──验证──→ 降噪 ──检索──→ 总结 ──注入──→ 升华 ──写入──→ 记忆
 ↑                                                                  │              │
 │                                                                  ↓              ↓
 └──────────── 反幻觉贯穿所有环节 ←────────────────────────── 幻觉数据库 ← Prompt 优化
```

- 每个环节写入记忆的内容，都会经过该环节的反幻觉检查
- 每个环节检索记忆的内容，都会被记忆中的反幻觉记录（已证伪列表、低信源列表、置信度评分）参考
- 每个环节产生的幻觉事件，都进入全局幻觉数据库
- 幻觉数据库的积累，反过来优化每个环节的 Prompt 和规则
- 形成"采集→验证→记忆→检索→推理→验证→记忆积累→Prompt 优化→更好采集"的持续进化闭环

### F021 AI 指标发现 Agent（P1）

**用户场景**：研究员不需要手动尝试各种指标参数，AI 自动分析当前市场状态，推荐最优指标及其参数组合。

**功能描述**：

- 分析标的的历史价格和成交量特征，识别市场状态（趋势/震荡/高波动/低波动）
- 根据市场状态自动推荐适用的技术指标和参数
- 自动生成合成指标（如将 RSI + 布林带宽度合并为"布林RSI强度指标"）
- 指标有效性评分：基于历史数据计算每个指标的信号质量（信噪比、胜率、盈亏比）
- 支持自定义指标通过 `@indicator` 装饰器注册

**验收标准**：

- 推荐指标在历史数据上的信号胜率 ≥ 55%
- 指标推荐耗时 < 10 秒/标的

### F022 AI 策略生成与配置 Agent（P0）

**用户场景**：用户用自然语言描述策略意图（如"在日线级别，当 MACD 金叉且东方财富论坛情绪超过 70% 时买入，仓位不超过总资产的 20%"），AI 自动生成可执行的策略代码。

**功能描述**：

- 自然语言 → 策略代码：基于 LLM 的 NL → Python 策略生成
- 策略意图解析：理解用户描述中的技术指标、条件、仓位、止损等要素
- 自动生成完整策略类（含 `on_start`、`on_bar`、`on_trade` 等钩子）
- 策略评分：AI 根据回测结果对策略进行多维度评分（收益、风险、夏普、过拟合风险）
- 策略推荐优化：自动建议参数修改、指标替换、风控调整
- 策略知识库：自动归档历史策略，支持相似策略检索

**验收标准**：

- 生成的策略代码无语法错误，可在引擎中正常运行
- 自然语言描述准确转译为策略要素（覆盖率 ≥ 90%）
- AI 评分与人工评估的一致性 ≥ 80%

### F023 AI 回测解读 Agent（P1）

**用户场景**：回测结束后，不是只看报表数字，AI 自动分析回测结果，指出问题、解释原因、给出改进建议。

**功能描述**：

- 自动解读回测结果：用自然语言描述策略表现（"该策略年化收益 25%，但最大回撤达 30%，主要集中在 2022 年 4 月和 2024 年 1 月，原因是这两次连续触发了跌停板无法卖出"）
- 过拟合检测：检测策略是否过拟合参数（参数敏感性分析、样本外表现对比）
- 策略改进建议：基于失败交易分析，提出具体优化方向（"建议增加 3 日缩量确认条件避免假突破"）
- 基准对比：与沪深 300 / 中证 500 对比，计算超额收益的来源（Alpha 分解）
- 生成可读的自然语言报告（非纯数字）
- **回测模式说明**：回测模式下 AI 仅做后分析（BacktestAgent），不实时介入交易流。AI 在回测结束后读取回测结果和策略日志，生成自然语言解读。回测期间 F020 信息处理全流程不执行（无实时新闻/情绪数据可处理）。

**验收标准**：

- 回测解读报告自动生成，包含至少 5 个维度的分析
- 过拟合检测准确率 ≥ 80%（与人工标记对比）
- 改进建议至少 3 条，且每条可操作

### F024 AI 实时盯盘 Agent（P1）

**用户场景**：盘中 AI 实时分析行情 + 消息面，自动识别交易机会和风险，通过钉钉推送提醒。

**功能描述**：

- 实时行情分析：监控指定股票/指数/板块的技术信号突破
- 消息面联动：实时新闻/公告与持仓标的联动分析（"持仓的贵州茅台发布Q3业绩预告，超预期 15%"）
- 情绪监控：社交媒体情绪突变检测（"东方财富论坛关于比亚迪的看空情绪突然升至 65%"）
- 异动检测：放量突破、涨停/跌停、异动成交量的实时告警
- AI 信号融合：将技术指标信号 + 情绪信号 + 基本面信号综合，输出最终交易建议
- 盘前简报：每日开盘前生成个股/板块分析简报
- 收盘总结：每日收盘后自动生成持仓总结和市场回顾

**验收标准**：

- 实时行情信号延迟 < 3 秒
- 消息面联动分析延迟 < 5 分钟
- AI 信号融合准确率 ≥ 65%（与人工标记对比）

### F025 AI 辅助决策 Agent（P0）

> **与 F024 的边界**：F024（盯盘 Agent）是**信号探测器**——负责发现机会/风险并推送通知，不直接参与交易决策。F025（决策 Agent）是**信号决策器**——在交易执行前对 F024 推送的信号和策略自身的信号做二次验证，决定是否执行、何时执行、执行多少仓位。

**用户场景**：在实盘/模拟盘交易中，AI 作为"第二大脑"提供决策建议——是否该买入/卖出、仓位是否合理、是否存在潜在风险。

**功能描述**：

- 交易建议生成：在每次交易决策前，AI 综合分析当前状态，给出建议（"建议买入 sh600519，理由：MACD 金叉 + 东方财富情绪转好 + 板块轮动到白酒"）
- 风险预警：识别潜在风险（"该标的已连续上涨 5 日，RSI=78，建议减少仓位"）
- 持仓优化建议：基于当前持仓组合，建议调整（"持仓中白酒占比 40%，建议降低至 20%"）
- 市场环境评估：当前市场处于什么状态（牛市/熊市/震荡），推荐相应的仓位水平
- 交易理由审计：每笔交易的 AI 生成理由记录到审计日志
- 信号验证：对 F024 推送的盯盘信号进行二次验证（技术面 + 消息面 + 情绪面交叉确认），过滤误报
- 信号优先级（明确与 F022 的关系）：
  1. **传统策略信号**（最高优先级）：`BaseStrategy.on_bar()` 生成的信号，作为基准
  2. **AI 辅助信号**（F025）：对传统策略信号的补充/否决，需用户确认
  3. **AI 生成策略信号**（F022 生成 + F020 升华）：需经过传统策略层验证后才能进入 F025 决策
- 人机协同模式：
  - **全自动**：AI 建议自动下单（需用户授权）
  - **半自动**：AI 建议 → 用户确认 → 下单
  - **只读**：AI 只推送建议，不自动下单

**验收标准**：

- AI 交易建议与策略信号的一致性 ≥ 70%（不矛盾）
- 风险预警触发后，策略可配置自动减仓或暂停
- 半自动模式下，用户确认平均响应时间 < 30 秒
- F024 推送信号经 F025 验证后误报率 ≤ 30%

### F026 AI 动态风控 Agent（P1）

**用户场景**：风控参数不是静态的，AI 根据市场环境动态调整——牛市放宽、熊市收紧。

**功能描述**：

- 动态风控参数：基于市场波动率动态调整仓位上限、止损线、回撤熔断阈值
- 异常交易检测：识别异常交易模式（频繁撤单、异常大额下单、同向连续下单）
- 黑天鹅防护：极端行情下自动降级为保守模式（大幅降低仓位、暂停新开仓）
- 相关性监控：实时计算持仓标的间的相关性，防止过度集中
- 风控报告：每周/月生成风控报告（触发次数、拦截原因、参数调整记录）
- 与市场环境联动：
  - 沪深 300 站上 200 日均线 → 仓位上限从 60% 升至 80%
  - VIX 类指标 > 30 → 自动触发减半仓位
  - 连续 3 日下跌 → 止损线从 10% 收紧至 5%
- A 股规则内建（AI 风控必须遵守 A 股约束）：
  - 动态调整止损线时不得违反 T+1 规则（T+1 期间不触发"当日卖出止损"）
  - 涨跌停时自动触发流动性告警（涨停板持仓占比 > 20% 时推送）
  - 建议减仓时自动计算 100 股整数倍的最接近数量

**验收标准**：

- 风控参数调整基于客观数据（市场指标），非纯黑盒
- 极端行情（单日跌幅 > 5%）下风控自动生效，延迟 < 10 秒
- 参数调整可审计（记录调整原因和依据）

### F027 AI 策略回测对比 Agent（P2）

**用户场景**：同时对比多个策略的优劣，AI 自动选出最优组合并推荐配置比例。

**功能描述**：

- 多策略横向对比：自动对比多个策略的各项指标，生成对比排名
- 策略组合优化：分析策略间相关性，推荐最优组合比例（降低组合回撤）
- 策略生命周期建议：基于近期表现，建议何时启用/停用某策略

### F028 AI 自然语言交互界面（P2）

**功能描述**：

- 对话式策略开发：用自然语言对话式创建、修改、回测策略
- 对话式数据分析："最近一个月白酒板块表现如何？" → AI 生成图表 + 分析
- 对话式回测报告："我的策略为什么在 3 月份亏了很多？" → AI 分析并回答
- 对话式盯盘："帮我监控 sh600519 和 sz000858" → AI 开始监控并推送通知
- 支持 Markdown + html 格式输出图表和表格

***

### F029 Web Dashboard 前端（P1）

> **定位**：StockQuant 2.0 的核心用户界面，通过浏览器提供完整的量化交易平台体验。React 18 + TypeScript + Vite + Ant Design 5 + ECharts。

**用户场景**：用户打开浏览器，登录 Dashboard，在回测页面选择数据和时间范围，配置策略参数，一键启动回测。回测过程中实时查看进度条和资金曲线。回测结束后查看完整指标报表、交易明细和 AI 解读报告。

**功能描述**：

**页面结构**（侧边栏导航 + 顶栏 + 内容区）：

| 页面    | 路由                   | 功能                                    | 核心组件                                                                                         |
| ----- | -------------------- | ------------------------------------- | -------------------------------------------------------------------------------------------- |
| 主页仪表盘 | `/`                  | 全局概览：权益曲线、持仓汇总、今日通知、系统状态              | EquityChart, MetricTable, NotificationList                                                   |
| 回测配置  | `/backtest`          | 数据选择 + 策略选择/编辑 + 参数配置 + 启动回测          | DataSelector, StrategyEditor, ParamForm, StartButton                                         |
| 回测结果  | `/backtest/:id`      | 30+ 指标汇总 + 资金曲线/回撤/热力图 + 交易明细 + AI 解读 | Chart/EquityChart, Chart/DrawdownChart, Chart/MonthHeatmap, Table/TradeTable, AI/InsightCard |
| 参数优化  | `/backtest/optimize` | 参数范围设置 + 并行优化 + 排名表 + 扫描图             | ParamForm, Table/ParameterRanking, ScanChart                                                 |
| 策略管理  | `/strategy`          | 策略列表 + 代码编辑器 + 语法高亮 + 保存/预览           | StrategyList, MonacoEditor, PreviewPanel                                                     |
| 实时盯盘  | `/monitor`           | 自选股列表 + 实时行情 + AI 信号推送 + 告警           | Monitor/StockTicker, Monitor/WatchList, Monitor/AlertPanel, AI/SignalCard                    |
| AI 对话 | `/ai-chat`           | 与 AI 对话：策略开发、数据分析、回测解读                | AI/ChatPanel, AI/SignalCard                                                                  |
| 投资组合  | `/portfolio`         | 持仓汇总 + 个股权益 + 行业分布 + 盈亏分析             | PortfolioSummary, SectorPieChart, PnLTable                                                   |
| 数据管理  | `/data`              | 数据源配置 + 缓存管理 + 数据采集历史                 | DataSourceForm, CacheStats, DataLogTable                                                     |
| 系统设置  | `/settings`          | LLM 配置 + AI Agent 开关 + 推送配置 + 风控参数    | LLMConfigForm, AgentToggles, NotifierForm                                                    |

**核心交互流程**：

1. **回测流程**：
   ```
   选择数据源（BaoStock/CSV）+ 标的 + 时间范围
   → 选择/新建策略（模板库 / Monaco 编辑器）
   → 配置参数 + 佣金/滑点 + 风控规则
   → 点击"运行回测" → 异步任务提交
   → WebSocket 实时推送进度条 + 中间指标
   → 完成 → 跳转结果页 → 自动渲染图表
   ```
2. **AI 对话流程**：
   ```
   用户输入自然语言
   → REST POST /api/ai/chat（发送消息）
   → 流式响应 SSE（Server-Sent Events）
   → ChatPanel 逐字渲染 AI 回复
   → 消息附带 SignalCard/InsightCard（如 AI 给出交易建议）
   ```
3. **实时盯盘流程**：
   ```
   WebSocket 连接 /ws/monitor
   → 接收实时行情 → ECharts 更新 K 线
   → 接收 AI 信号推送 → SignalCard 弹出提醒
   → 用户点击信号 → 查看 AI 推理链
   → 用户确认 → 提交订单（半自动模式）
   ```

**系统设置页 Settings 详细设计**（参考 autoquant 项目 Settings.vue UI + 配置参数结构）：

> 设计依据：`D:\projects\autoquant\frontend\src\views\Settings.vue` + `D:\projects\autoquant\src\utils\runtime.py`（ALLOWED\_SETTINGS 白名单 14 分组体系）。UI 复刻暗色科技感主题、向导/专家模式双视图、动态表单、条件显隐、密钥掩码、dirty 追踪、浮动保存条、管理员口令二次确认。

**UI 架构**（与 autoquant 一致）：

```
┌──────────────────────────────────────────────────┐
│ [紫色横幅] 运行配置中心 │ 所有修改保存并热生效      │
│  描述 + TRADING_ADMIN_TOKEN 说明                 │
├──────────────────────────────────────────────────┤
│ [工具栏] 向导模式│专家模式  │ 全部展开│全部折叠   │
├──────────────────────────────────────────────────┤
│                                                  │
│  专家模式：                                     │
│  ┌───┐                                          │
│  │ ▼ 系统总控 [6] │ 监控图标                     │
│  │  trading.mode  │ 交易模式  [select:模拟]      │
│  │  system.log_   │ 日志级别  [select:INFO]      │
│  │  ... 其余项...                          │     │
│  │                                            │  │
│  │ [批量保存(2)] [放弃修改]                  │  │
│  └────────────────────────────────────────────┘ │
│  ┌───┐                                          │
│  │ ▼ 风控阈值 [5] │ 警告图标                     │
│  │  risk_control.max_  │ 单笔止损  [0.08] 8% ▸  │
│  │  ... 其余项...                          │     │
│  │ [批量保存(0)] [放弃修改]                  │  │
│  └────────────────────────────────────────────┘ │
│                                                  │
│  向导模式：                                     │
│  [1 数据源] → [2 交易] → [3 风控] → [4 AI] → ... │
│                                                  │
├──────────────────────────────────────────────────┤
│ [底部栏] ⚠️ 已修改 3 项  │  重新加载  [保存并生效] │
├──────────────────────────────────────────────────┤
│ [浮动条] ⚠️ 3 项配置未保存  [全部放弃] [全部保存]  │
│                          (右下角固定)             │
└──────────────────────────────────────────────────┘
```

**14 个配置分组**（按 StockQuant 2.0 实际功能精简参数）：

| 序号 | 分组 key                | 组名    | 图标         | 精简后参数项                                                                                                                                                                                                                                           | autoquant 原分组             |
| -- | --------------------- | ----- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------- |
| 1  | `system_control`      | 系统总控  | Monitor    | trading.mode, system.log\_level, system.web\_port, system.initial\_capital, simulator.force\_run, simulator.tick\_interval, ssl.enabled                                                                                                          | system\_control + ssl     |
| 2  | `data_source`         | 数据源   | Coin       | data\_provider.source, baostock.enabled, data\_provider.default\_api\_key, data\_provider.default\_api\_url, data\_provider.tushare\_token, data\_provider.tdx\_host, data\_provider.tdx\_port, data\_provider.duckdb\_path, mysql/postgres 连接参数 | data\_source + baostock   |
| 3  | `trading_cost`        | 交易成本  | Wallet     | system.commission\_rate, system.min\_commission, system.stamp\_tax\_rate, execution.transfer\_fee\_rate                                                                                                                                          | trading\_cost             |
| 4  | `execution_params`    | 执行参数  | Aim        | system.slippage, system.lot\_size, system.price\_limit\_ratio                                                                                                                                                                                    | execution\_params         |
| 5  | `trading_session`     | 交易时段  | Clock      | system.morning\_open, system.morning\_close, system.afternoon\_open, system.afternoon\_close                                                                                                                                                     | trading\_session          |
| 6  | `broker_channel`      | 券商通道  | Connection | trading.broker, trading.lock\_on\_startup, trading.poll\_interval\_sec, trading.auto\_reconcile\_minutes, qmt 参数, broker\_http 参数                                                                                                                | broker\_channel           |
| 7  | `risk_control`        | 风控阈值  | Warning    | risk\_control.max\_stop\_loss\_pct, max\_pos\_per\_stock, max\_total\_pos, max\_daily\_loss\_pct, max\_drawdown\_pct                                                                                                                             | risk\_control             |
| 8  | `ai_model`            | AI 模型 | MagicStick | ai\_model.provider, api\_url, api\_key, model, temperature, max\_tokens, timeout\_sec, retry\_times, fallback\_to\_mock, strategy\_llm\_\*                                                                                                       | ai\_model + strategy\_llm |
| 9  | `evolution`           | 策略进化  | Promotion  | evolution.enabled, llm\_provider, llm\_base\_url, llm\_api\_key, llm\_model, llm\_temperature, llm\_max\_tokens, llm\_timeout, llm\_retry, llm\_fallback                                                                                         | evolution                 |
| 10 | `notification`        | 通知推送  | Bell       | notification.wechat\_webhook, dingtalk\_webhook, smtp\_*, email\_*, telegram\_bot\_token                                                                                                                                                         | notification              |
| 11 | `fundamental_adapter` | 基本面适配 | Document   | fundamental\_adapter.enabled, apply\_in\_backtest, apply\_in\_live, provider, cache\_ttl, refresh\_interval                                                                                                                                      | fundamental\_adapter      |
| 12 | `signal`              | 信号管理  | Connection | signal.dedup\_cooldown\_sec, dedup\_db\_check\_pending, dedup\_audit\_rejected                                                                                                                                                                   | signal                    |
| 13 | `history_sync`        | 历史同步  | Refresh    | history\_sync.write\_mode, interval\_minutes, scheduler\_start\_time, lookback\_days, concurrency, stock\_list\_provider                                                                                                                         | history\_sync             |
| 14 | `kafka_messaging`     | 消息总线  | Promotion  | kafka.enabled, bootstrap\_servers, consumer\_group, topic\_\*                                                                                                                                                                                    | kafka\_messaging          |

**后台 API 设计**（Settings API）：

```
GET    /api/settings           # 返回完整配置：分组信息 + 所有条目（含值 + 完整 UI 元数据）
POST   /api/settings/save      # 批量保存 { admin_token, items: [{key, value}] }
DELETE /api/settings/:key       # 删除覆盖，恢复 .env 默认值
GET    /api/settings/whitelist  # 仅暴露白名单结构（不含 value），用于动态渲染
```

**响应格式**：

```json
{
  "success": true,
  "groups": [
    {"key": "risk_control", "label": "风控阈值", "icon": "Warning", "count": 5, "order": 1},
    {"key": "data_source", "label": "数据源", "icon": "Coin", "count": 10, "order": 2}
  ],
  "items": [
    {
      "key": "risk_control.max_stop_loss_pct",
      "value": 0.08,
      "overridden": false,
      "value_type": "float",
      "label": "单笔止损",
      "description": "单笔最大止损比例",
      "secret": false,
      "group_label": "风控阈值",
      "group_icon": "Warning",
      "order": 1,
      "min": 0.001, "max": 0.3, "step": 0.001,
      "scale": 100, "unit": "%", "slider": true,
      "options": [],
      "when": null
    }
  ]
}
```

**前端 Settings 页面实现**：

- **UI 框架**：React 18 + Ant Design 5（复刻 Element Plus 布局）
- **暗色主题**：CSS Variables + 紫色渐变横幅 + 毛玻璃效果（复刻 autoquant 的 `.cc-banner` / `.cc-toolbar` / `.cc-footer-bar` 风格）
- **专家模式**：Ant Design Collapse 组件折叠分组，每行 grid 布局（label | control | status | action）
- **向导模式**：Ant Design Steps 分步导航（1 数据源 → 2 交易 → 3 风控 → 4 AI → 5 通知 → 6 高级）
- **动态控件**：根据 `value_type` 渲染不同组件（select → Select, boolean → Switch, password → Input.Password, number → InputNumber + Slider, time → TimePicker, lines → Textarea）
- **条件显隐**：`when` 字段定义依赖关系（如 `when: {field: "trading.broker", values: ["qmt"]}`）
- **密钥掩码**：`secret` 字段默认显示 `********`，编辑后显示真实值
- **Dirty 追踪**：修改 → 显示"未保存"标签；批量保存/放弃修改
- **浮动保存条**：右下角固定，显示未保存数量和操作按钮
- **管理员口令**：保存/删除操作弹出 Modal 要求输入 admin\_token

***

**技术实现**：

- **API 客户端**：Axios + 拦截器（请求加 Token、响应统一错误处理、自动刷新 Token）
- **状态管理**：Zustand（轻量、无 boilerplate，适合中等复杂度应用）
- **图表渲染**：ECharts 5（K 线、资金曲线、回撤、热力图、行业分布饼图）
- **代码编辑器**：Monaco Editor（VS Code 同款，支持 TypeScript 语法高亮 + 智能提示）
- **实时通信**：
  - WebSocket：回测进度、实时行情、盯盘推送
  - SSE：AI 对话流式输出
- **响应式设计**：Ant Design 栅格系统，支持 1280px+ 桌面端（暂不兼容移动端）

**验收标准**：

- 首屏加载时间 < 3 秒（Lighthouse Performance ≥ 80）
- 回测结果页图表渲染 < 2 秒（1000 笔交易以内）
- 实时行情推送延迟 < 500ms（从数据到达页面更新）
- 所有页面中文渲染正确，无乱码
- Monaco Editor 语法高亮准确率 100%

***

### F030 前后端集成部署（P1）

**功能描述**：

- **Docker Compose 一键部署**：
  ```yaml
  # docker-compose.yml
  services:
    frontend:       # React SPA (Nginx 静态文件服务)
      build: ./web
      ports: ["80:80"]
    backend:        # FastAPI (Uvicorn)
      build: .
      ports: ["8000:8000"]
      depends_on: [postgres, chromadb]
    postgres:       # PostgreSQL (持久化存储)
      image: postgres:16-alpine
    chromadb:       # 向量数据库（AI 记忆）
      image: chromadb/chroma:latest
  ```
- **Nginx 反向代理**：前端 `/` → Nginx SPA，后端 `/api/*` → FastAPI，WebSocket `/ws/*` → FastAPI
- **前端构建**：`npm run build` → `dist/` 目录，Nginx  serving 静态文件
- **后端构建**：`pip install -e .` → Uvicorn Gunicorn 多进程
- **环境变量**：`.env.production`（API 地址、LLM Key、数据库 URL）
- **健康检查**：Nginx 健康端点 `/health`，Docker Compose healthcheck

**验收标准**：

- `docker compose up -d` 一键启动全部服务
- 前后端通信正常（前端可访问后端 API）
- Nginx 反向代理配置正确（CORS 已处理）

### NFR001 性能

| 场景                | 要求                     |
| ----------------- | ---------------------- |
| 日线回测（10 年，5 只股票）  | ≥ 5000 Bar/秒（单核）       |
| 分钟线回测（10 年，5 只股票） | ≥ 500 Bar/秒（单核）        |
| 参数优化（100 组，4 核）   | ≤ 单组时间 × 4（并行效率 ≥ 75%） |
| 指标计算              | 18 个指标全部实现，单次计算 < 1ms  |
| 数据缓存读取            | 读取 10 年日线 < 100ms      |
| 风控检查              | < 1ms/订单               |
| 策略执行延迟（Tick 级）    | < 10ms（从 Tick 到订单）     |

### NFR002 可靠性

| 要求    | 描述                      |
| ----- | ----------------------- |
| 事件派发  | 零丢失，通过集成测试保证            |
| 订单处理  | 幂等性保证（重复订单自动去重）         |
| 数据完整性 | 数据下载失败自动重试（最多 3 次，指数退避） |
| 崩溃恢复  | 模拟盘/实盘模式重启后从缓存恢复状态      |
| 回测确定性 | 同一参数同一策略同一数据，多次运行结果完全一致 |

### NFR003 可扩展性

| 要求     | 描述                                     |
| ------ | -------------------------------------- |
| 数据源    | 新增数据源只需实现 `DataFeed` 接口                |
| Broker | 新增 Broker 只需实现 `Broker` 接口             |
| 指标     | 新增指标通过 `@indicator` 装饰器或继承 `Indicator` |
| 策略     | 新增策略只需继承 `BaseStrategy`                |
| 推送     | 新增推送渠道只需实现 `Notifier` 接口               |
| 并行     | 参数优化支持跨机器分布式（未来）                       |

### NFR004 安全性

| 要求   | 描述                        |
| ---- | ------------------------- |
| 配置加密 | API Key、Token 等敏感信息支持加密存储 |
| 交易审计 | 所有订单操作记录审计日志              |
| 风控熔断 | 异常行情自动暂停交易                |
| 权限控制 | Web Dashboard 支持用户角色（未来）  |

### NFR005 可维护性

| 要求     | 描述                    |
| ------ | --------------------- |
| 测试覆盖率  | ≥ 90%（核心模块 100%）      |
| 代码规范   | 遵循 PEP 8，类型标注 100% 覆盖 |
| API 文档 | 自动生成 API 文档（Sphinx）   |
| 变更日志   | 每次发布更新 CHANGELOG      |

### NFR006 兼容性

| 要求     | 描述                                  |
| ------ | ----------------------------------- |
| Python | 3.10+                               |
| 操作系统   | Windows 10+、macOS 12+、Ubuntu 20.04+ |
| 数据库    | SQLite（内置）、PostgreSQL（可选）           |

### NFR007 可用性

| 要求   | 描述                            |
| ---- | ----------------------------- |
| 安装   | `pip install stockquant` 一键安装 |
| 上手   | 5 分钟完成第一个回测（文档 + 示例）          |
| 文档   | 完整的 Getting Started 教程        |
| 中文友好 | 全部中文文档和错误提示                   |

### NFR008 AI 性能与成本

| 要求            | 描述                                            |
| ------------- | --------------------------------------------- |
| 数据采集延迟        | 从发布到入库 < 5 分钟                                 |
| 情感分析延迟        | 单篇文章 < 100ms（本地模型）或 < 500ms（LLM）              |
| AI 决策延迟（轻量模式） | 本地模型/规则引擎响应 < 200ms（实盘高频场景，不使用 LLM）           |
| AI 决策延迟（完整模式） | LLM 调用 + 记忆检索 + 反幻觉验证 < 3 秒（实盘低频/回测场景）        |
| AI 决策模式自动切换   | 高频 Tick 级使用本地模型（< 200ms），Bar 级/日级使用 LLM（< 3s） |
| LLM 调用成本      | 每日 ≤ 2 元（默认配置下，轻量模式为主）                        |
| 降级策略          | LLM API 不可用时自动降级为本地模型 → 规则引擎                  |
| 本地模型可选        | 支持 HuggingFace 本地模型推理，零 API 成本                |

> **性能互斥解决**：实盘高频场景（Tick 级）仅使用本地模型/规则引擎（< 200ms），不触发 LLM 调用。LLM 调用仅发生在低频场景（Bar 级/日级/事件触发），允许 < 3 秒延迟。回测模式下 AI 仅做后分析（BacktestAgent），不拦截交易流。

### NFR009 AI 可靠性

| 要求          | 描述                                  |
| ----------- | ----------------------------------- |
| 情感分析准确率     | ≥ 75%（与人工标注对比）                      |
| 信息抽取准确率     | ≥ 85%（实体识别 + 事件分类）                  |
| AI 信号一致性    | AI 建议与策略信号的一致性 ≥ 70%（不矛盾）           |
| LLM 输出可追溯   | 所有 LLM 调用记录 Prompt 和 Response 到审计日志 |
| 可解释性        | AI 决策必须附带推理过程（Chain-of-Thought）     |
| 幻觉防护        | AI 生成的交易建议必须引用数据来源，无来源标注时标记为"低置信度"  |
| 人工确认        | 全自动模式下关键操作仍需人工确认（防 LLM 幻觉）          |
| **事实验证通过率** | ≥ 99%（AI 输出的数据与数据源一致）               |
| **来源验证准确率** | 对已采集数据源 100% 准确                     |
| **推理幻觉检出率** | ≥ 80%（与人工标记对比）                      |
| **用户纠正确认率** | 纠正后同类错误再犯率 ≤ 5%                     |
| **连续幻觉告警**  | 连续 3 次幻觉自动触发紧急模式                    |

***

## 5. 系统架构

### 5.1 包结构

```
stockquant/
├── __init__.py                    # 公共 API 导出
├── engine/                        # 回测引擎层
│   ├── __init__.py
│   ├── cerebro.py                # Cerebro 主引擎
│   ├── event.py                  # EventEngine + Event 定义
│   ├── portfolio.py              # 投资组合管理
│   ├── oms.py                    # 订单管理系统
│   ├── broker.py                 # Broker ABC
│   │   ├── backtest_broker.py
│   │   ├── paper_broker.py
│   │   └── live_broker.py
│   ├── commission.py             # 佣金/滑点模型
│   ├── risk.py                   # 风险管理
│   └── metrics.py                # 回测统计 30+ 指标
├── strategy/                      # 策略框架层
│   ├── __init__.py
│   ├── base.py                   # BaseStrategy
│   ├── signal.py                 # 信号系统
│   └── templates/                # 内置策略模板
│       ├── ma_crossover.py
│       ├── bollinger.py
│       ├── macd_divergence.py
│       ├── rsi_reversal.py
│       ├── dual_thrust.py
│       ├── mean_reversion.py
│       └── momentum.py
├── analytics/                     # 分析报表层
│   ├── __init__.py
│   ├── report.py                 # HTML/PDF/JSON 报表
│   └── plotter.py                # Plotly 可视化
├── data/                          # 数据层
│   ├── __init__.py
│   ├── feed.py                   # DataFeed ABC
│   ├── cache.py                  # DataCache
│   ├── providers/                # 结构化数据源
│   │   ├── baostock_feed.py
│   │   ├── tushare_feed.py
│   │   ├── netease_feed.py
│   │   ├── csv_feed.py
│   │   ├── parquet_feed.py
│   │   └── sqlite_feed.py
│   └── models/                   # 数据模型
│       ├── kline.py              # K 线数据类
│       ├── tick.py               # Tick 数据类
│       ├── order.py              # 订单数据类
│       ├── position.py           # 持仓数据类
│       └── sentiment.py          # 情绪数据类（AI）
├── indicators/                    # 技术指标层
│   ├── __init__.py
│   ├── base.py                   # Indicator ABC
│   ├── moving_avg.py             # MA/EMA/KAMA/TRIX
│   ├── oscillators.py            # RSI/KDJ/CCI/ROC/STOCHRSI
│   ├── volatility.py             # BOLL/ATR/STDDEV/SAR
│   ├── trend.py                  # MACD/OBV
│   └── utils.py                  # 工具函数
├── execution/                     # 交易执行层
│   ├── __init__.py
│   ├── trade.py                  # LiveBroker（券商 API）
│   └── notifier/                 # 消息推送
│       ├── base.py
│       ├── dingtalk.py
│       ├── email.py
│       ├── wechat.py
│       └── telegram.py
├── ai/                            # AI 层（新增）
│   ├── __init__.py
│   ├── orchestrator.py           # AI Agent 编排器
│   ├── pipeline.py               # 信息处理全流程编排（采集→降噪→总结→升华）
│   ├── data_agent.py             # 信息采集 Agent（含来源验证+事实初筛）
│   ├── denoiser.py               # 信息降噪器（信源降权+时效过滤+一致性过滤+冗余压缩）
│   ├── summarizer.py             # 信息总结器（记忆检索+反幻觉验证+记忆写入）
│   ├── elevator.py               # 信息升华器（多源融合+推理链验证+交叉验证）
│   ├── indicator_agent.py        # 指标发现 Agent
│   ├── strategy_agent.py         # 策略生成 Agent
│   ├── backtest_agent.py         # 回测解读 Agent
│   ├── monitor_agent.py          # 实时盯盘 Agent
│   ├── decision_agent.py         # 辅助决策 Agent
│   ├── risk_agent.py             # 动态风控 Agent
│   ├── memory/                   # 记忆系统（被全流程各环节调用）
│   │   ├── base.py               # 记忆层 ABC（L1/L2/L3）
│   │   ├── working.py            # 工作记忆（会话级，内存）
│   │   ├── short_term.py         # 短期记忆（向量数据库 + 时间索引）
│   │   ├── long_term.py          # 长期记忆（持久化 + 语义索引）
│   │   ├── memory_lifecycle.py        # 记忆生命周期管理（总结/遗忘/压缩）
│   │   ├── forgetter.py          # 遗忘机制（时效性 + 置信度 + 冗余压缩）
│   │   ├── compressor.py         # 记忆压缩器
│   │   └── retriever.py          # 多源检索器（语义 + 关键词 + 时间 + 置信度）
│   ├── hallucination/            # 反幻觉系统（被全流程各环节调用）
│   │   ├── base.py               # 幻觉检测器 ABC
│   │   ├── source_verifier.py    # 来源验证（采集时）
│   │   ├── fact_screener.py      # 事实初筛（采集时）
│   │   ├── consistency_filter.py # 一致性过滤（降噪时）
│   │   ├── prompt_constraint.py  # Prompt 约束注入（总结时）
│   │   ├── post_verify.py        # 总结后验证（五步中的事实+来源验证）
│   │   ├── logic_verifier.py     # 推理链验证（升华时）
│   │   ├── cross_validator.py    # 交叉验证（升华时）
│   │   ├── confidence_scorer.py  # 置信度评分（全部环节输出点）
│   │   ├── hallucination_db.py   # 幻觉数据库（全局积累）
│   │   ├── pattern_detector.py   # 幻觉模式识别
│   │   ├── prompt_optimizer.py   # Prompt 自动优化
│   │   └── feedback_loop.py      # 人工纠正反馈循环
│   ├── llm/                      # LLM 适配层
│   │   ├── base.py               # LLM Provider ABC
│   │   ├── openai.py             # OpenAI GPT
│   │   ├── anthropic.py          # Claude
│   │   ├── local.py              # 本地模型（Ollama）
│   │   └── config.py             # LLM 配置
│   ├── scrapers/                 # 数据采集器
│   │   ├── base.py               # Scraper ABC
│   │   ├── eastmoney.py          # 东方财富
│   │   ├── xueqiu.py             # 雪球
│   │   ├── cls.py                # 财联社
│   │   ├── cninfo.py             # 巨潮资讯
│   │   └── sszi.py               # 上交所
│   ├── nlp/                      # NLP 处理
│   │   ├── sentiment.py          # 情感分析
│   │   ├── extractor.py          # 信息抽取
│   │   └── classifier.py         # 分类器
│   └── storage/                  # AI 数据存储
│       ├── sqlite_store.py       # SQLite 存储
│       └── pg_store.py           # PostgreSQL 存储
├── utils/                         # 工具层
│   ├── __init__.py
│   ├── time.py                   # 时间工具
│   ├── logger.py                 # 日志
│   └── config.py                 # 配置管理
└── __main__.py                   # CLI 入口
├── api/                            # API 网关层（新增 — FastAPI）
│   ├── __init__.py
│   ├── main.py                     # FastAPI 应用入口 + 中间件
│   ├── routers/                    # RESTful 路由
│   │   ├── backtest.py             # 回测相关（提交/查询/删除/列表）
│   │   ├── strategy.py             # 策略管理（CRUD + 预览）
│   │   ├── data.py                 # 数据管理（数据源配置 + 缓存管理）
│   │   ├── dashboard.py            # Dashboard 数据接口（持仓/收益/指标）
│   │   ├── ai.py                   # AI 相关（对话/分析/盯盘配置）
│   │   ├── user.py                 # 用户认证（未来多用户）
│   │   └── system.py               # 系统状态（健康检查/版本/资源）
│   ├── websocket.py                # WebSocket 实时推送（行情/交易/通知）
│   ├── deps.py                     # 依赖注入（认证/配置）
│   └── schemas.py                  # Pydantic 数据模型
├── web/                            # Web 前端（新增 — React + Ant Design）
│   ├── package.json                # 前端依赖
│   ├── vite.config.ts              # Vite 构建配置
│   ├── tsconfig.json               # TypeScript 配置
│   ├── src/
│   │   ├── main.tsx                # 应用入口
│   │   ├── App.tsx                 # 路由配置 + 布局
│   │   ├── store/                  # 状态管理（Zustand）
│   │   │   ├── index.ts
│   │   │   ├── marketStore.ts      # 市场行情状态
│   │   │   ├── backtestStore.ts    # 回测任务状态
│   │   │   ├── strategyStore.ts    # 策略管理状态
│   │   │   ├── aiStore.ts          # AI 对话状态
│   │   │   └── notificationStore.ts # 通知状态
│   │   ├── api/                    # API 客户端
│   │   │   ├── client.ts           # Axios 实例 + 拦截器
│   │   │   ├── backtest.ts         # 回测 API 调用封装
│   │   │   ├── strategy.ts         # 策略 API 调用封装
│   │   │   ├── dashboard.ts        # Dashboard API 调用封装
│   │   │   ├── ai.ts               # AI 对话 API 调用封装
│   │   │   └── websocket.ts        # WebSocket 客户端
│   │   ├── components/             # 公共组件
│   │   │   ├── Layout.tsx          # 主布局（侧边栏 + 顶栏 + 内容区）
│   │   │   ├── Chart/              # 图表组件
│   │   │   │   ├── EquityChart.tsx       # 资金曲线（ECharts）
│   │   │   │   ├── DrawdownChart.tsx     # 回撤曲线
│   │   │   │   ├── MonthHeatmap.tsx      # 月度收益热力图
│   │   │   │   └── IndexComparison.tsx   # 基准对比
│   │   │   ├── Table/              # 表格组件
│   │   │   │   ├── TradeTable.tsx        # 交易明细（分页/排序/筛选）
│   │   │   │   ├── MetricTable.tsx       # 指标汇总表
│   │   │   │   └── ParameterRanking.tsx  # 参数优化排名
│   │   │   ├── AI/                 # AI 交互组件
│   │   │   │   ├── ChatPanel.tsx         # AI 对话面板
│   │   │   │   ├── SignalCard.tsx        # AI 信号卡片
│   │   │   │   └── InsightCard.tsx       # AI 洞察卡片
│   │   │   ├── Monitor/            # 盯盘组件
│   │   │   │   ├── StockTicker.tsx       # 实时行情滚动条
│   │   │   │   ├── WatchList.tsx         # 自选股列表
│   │   │   │   └── AlertPanel.tsx        # 告警面板
│   │   │   └── common/             # 通用组件
│   │   │       ├── PageHeader.tsx
│   │   │       ├── StatusBadge.tsx
│   │   │       └── ConfirmModal.tsx
│   │   ├── pages/                  # 页面组件
│   │   │   ├── Dashboard.tsx         # 主页仪表盘
│   │   │   ├── Backtest.tsx          # 回测配置与执行页
│   │   │   ├── BacktestResult.tsx    # 回测结果详情页
│   │   │   ├── Strategy.tsx          # 策略编辑器页
│   │   │   ├── Monitor.tsx           # 实时盯盘页
│   │   │   ├── AIChat.tsx            # AI 对话页
│   │   │   ├── Portfolio.tsx         # 投资组合页
│   │   │   ├── DataManage.tsx        # 数据管理页
│   │   │   ├── Settings.tsx          # 系统设置页
│   │   │   └── Login.tsx             # 登录页（未来）
│   │   ├── types/                  # TypeScript 类型定义
│   │   │   ├── backtest.ts         # 回测相关类型
│   │   │   ├── strategy.ts         # 策略相关类型
│   │   │   ├── market.ts           # 行情相关类型
│   │   │   └── ai.ts               # AI 相关类型
│   │   ├── hooks/                  # 自定义 Hooks
│   │   │   ├── useWebSocket.ts     # WebSocket 连接管理
│   │   │   ├── useBacktest.ts      # 回测任务控制
│   │   │   └── useAIChat.ts        # AI 对话管理
│   │   └── utils/                  # 工具函数
│   │       ├── formatters.ts       # 格式化工具（价格/百分比/时间）
│   │       └── validators.ts       # 表单校验
│   └── public/                     # 静态资源
│       └── favicon.ico
```

### 5.2 Web 前端架构

StockQuant 2.0 前端采用 **React 18 + TypeScript + Vite** 技术栈，组件库使用 **Ant Design 5**，图表库使用 **ECharts**。

**架构分层**：

```
┌──────────────────────────────────────────────────────┐
│                    Web 浏览器                         │
│                                                      │
│  React Router (路由)                                  │
│  ├── 侧边栏导航 (Dashboard / 回测 / 盯盘 / AI 对话)   │
│  ├── 顶栏 (用户/通知/设置)                             │
│  └── 内容区（动态加载页面）                            │
│                                                      │
│  Zustand (状态管理 — 轻量 Redux 替代)                  │
│  ├── marketStore → 实时行情、自选股                    │
│  ├── backtestStore → 回测任务进度/结果                 │
│  ├── strategyStore → 策略 CRUD                        │
│  ├── aiStore → AI 对话历史                           │
│  └── notificationStore → 推送通知                    │
│                                                      │
│  页面组件 (pages/)                                    │
│  ├── Dashboard.tsx  → 主页仪表盘                      │
│  ├── Backtest.tsx   → 回测配置 + 执行                  │
│  ├── Monitor.tsx    → 实时盯盘                        │
│  ├── AIChat.tsx     → AI 对话                        │
│  └── ...                                            │
│                                                      │
│  公共组件 (components/)                               │
│  ├── Chart/  (资金曲线/回撤/热力图/基准对比)           │
│  ├── Table/  (交易明细/指标表/参数排名)                │
│  ├── AI/     (对话面板/信号卡片/洞察卡片)              │
│  └── Monitor/ (行情滚动条/自选股/告警)                 │
│                                                      │
│  API 层                                             │
│  ├── Axios REST 客户端 (异步请求)                     │
│  └── WebSocket 客户端 (实时行情/通知/回测进度)        │
└──────────────────────────────────────────────────────┘
```

**前后端交互模式**：

```
浏览器 (React)                  FastAPI 后端                  量化引擎
  │                                │                            │
  │  HTTP POST /api/backtest      │                            │
  │  ← 202 {task_id: "xxx"}       │                            │
  │                                │                            │
  │  WS connect /ws/backtest/xxx  │──── 启动回测任务 ──→       │
  │                                │                            │
  │  WS ← {"type": "progress",    │←── on_bar 派发 ──→        │
  │              "progress": 45}   │    策略执行                │
  │                                │                            │
  │  WS ← {"type": "metrics",     │← on_finish 聚合 ──→       │
  │              "sharpe": 1.2}    │    指标计算                 │
  │                                │                            │
  │  HTTP GET /api/backtest/xxx   │── 返回结果 ──→             │
  │  ← {metrics, trades, equity}  │                            │
```

**实时数据流**：

```
行情数据 → NetEase/Tushare → DataCache → WebSocketHandler (FastAPI)
  → WS broadcast → browser (useWebSocket hook)
  → marketStore.update() → 页面自动更新（ECharts 实时渲染）
```

### 5.3 API 网关（FastAPI）

**职责**：前后端之间的唯一通信桥梁，提供 RESTful API + WebSocket 实时推送。

**API 设计**：

| 路由                              | 方法             | 描述                   |
| ------------------------------- | -------------- | -------------------- |
| `/api/health`                   | GET            | 健康检查                 |
| `/api/backtest`                 | POST           | 提交回测任务               |
| `/api/backtest`                 | GET            | 回测任务列表               |
| `/api/backtest/{id}`            | GET            | 回测结果（指标 + 交易 + 权益曲线） |
| `/api/backtest/{id}`            | DELETE         | 删除回测任务               |
| `/api/backtest/optimize`        | POST           | 提交参数优化任务             |
| `/api/strategy`                 | POST           | 创建策略                 |
| `/api/strategy`                 | GET            | 策略列表                 |
| `/api/strategy/{id}`            | GET/PUT/DELETE | 策略 CRUD              |
| `/api/data/sources`             | GET/POST       | 数据源配置                |
| `/api/data/cache`               | GET            | 缓存状态                 |
| `/api/dashboard/metrics`        | GET            | 仪表盘核心指标              |
| `/api/ai/chat`                  | POST           | AI 对话（SSE 流式响应）      |
| `/api/ai/analyze-backtest/{id}` | POST           | AI 解读回测结果            |
| `/api/monitor/config`           | PUT            | 盯盘配置                 |
| `/ws/backtest/{task_id}`        | WS             | 回测实时进度               |
| `/ws/monitor`                   | WS             | 实时行情/盯盘推送            |
| `/ws/notification`              | WS             | 系统通知/告警              |

**WebSocket 消息格式**：

```json
{
  "type": "progress|metrics|trade|alert|quote",
  "task_id": "xxx",
  "data": { ... },
  "timestamp": "2026-06-14T10:30:00Z"
}
```

**认证**：

- JWT Token 认证（Bearer Token）
- CORS 配置：前端域名白名单
- Rate Limiting：API 调用频率限制（默认 100 req/min）

### 5.4 核心数据流

```
回测/实盘数据流：
用户策略 → Cerebro → EventEngine → BarEvent → BaseStrategy.on_bar()
  → Broker.place_order() → RiskManager.check() → 撮合引擎 → 更新 Portfolio
  → 派发 TradeEvent → Strategy.on_trade() → 更新 Metrics
  → 回测结束 → ReportGenerator → HTML/PDF 报表

AI 数据流：
DataAgent 采集新闻/公告/研报 → NLP 情感分析 → Sentiment数据入库
  → MonitorAgent 实时读取 Sentiment + K-line
    → DecisionAgent 融合技术面 + 消息面 → 生成交易建议
      → 推送用户（半自动）或 提交策略（全自动）
  → BacktestAgent 读取回测结果 → 生成自然语言解读
```

### 5.3 核心类关系

```
Cerebro (主引擎)
  ├── EventEngine (事件调度器)
  │     ├── TimerEvent → on_timer()
  │     ├── BarEvent → strategy.on_bar(bar)
  │     ├── TickEvent → strategy.on_tick(tick)
  │     ├── TradeEvent → strategy.on_trade(trade)
  │     ├── OrderEvent → strategy.on_order(order)
  │     └── AccountEvent → strategy.on_account(account)
  ├── Portfolio (投资组合)
  │     ├── CashManager
  │     └── PositionManager {symbol: Position}
  ├── OMS (订单管理)
  │     ├── Order (状态机)
  │     └── Executor (撮合)
  ├── Broker ABC
  │     ├── BacktestBroker
  │     ├── PaperBroker
  │     └── LiveBroker
  ├── RiskManager
  ├── Metrics (30+ 指标)
  └── CommissionInfo + SlippageModel

AI Agent Orchestrator (AI 中枢)
  ├── DataAgent
  │     ├── Scrapers (共享基础设施，见 ai/scrapers/)
  │     │   ├── EastMoneyScraper
  │     │   ├── XueQiuScraper
  │     │   ├── CLSScraper
  │     │   └── CNInfScraper
  │     ├── NLP Pipeline
  │     │   ├── SentimentAnalyzer
  │     │   ├── InfoExtractor
  │     │   └── Classifier
  │     └── DataStore
  ├── IndicatorAgent
  ├── StrategyAgent
  │     └── LLM Adapter
  │           ├── OpenAILLM
  │           ├── AnthropicLLM
  │           └── LocalLLM (Ollama)
  ├── BacktestAgent
  ├── MonitorAgent
  ├── DecisionAgent
  │     └── SignalFusion (技术指标 + 情绪信号融合)
  └── RiskAgent

InformationProcessingPipeline (信息处理全流程 — 核心编排)
  ├── CollectionStage (环节1: 信息采集)
  │     ├── Scrapers (共享，引用 ai/scrapers/)
  │     │   ├── EastMoneyScraper
  │     │   ├── XueQiuScraper
  │     │   ├── CLSScraper
  │     │   └── CNInfScraper
  │     ├── SourceVerifier (来源验证) ──→ 反幻觉
  │     ├── FactScreener (事实初筛) ──→ 反幻觉
  │     ├── Deduplicator (去重) ──→ 记忆(L2)
  │     └── NoiseFilter (噪音过滤) ──→ 记忆(L3低信源)
  ├── DenoiseStage (环节2: 信息降噪)
  │     ├── SourceRanker (信源降权) ──→ 记忆(L3信用度)
  │     ├── TemporalFilter (时效过滤) ──→ 记忆(L3有效期)
  │     ├── ConsistencyFilter (一致性过滤) ──→ 反幻觉(已证伪)
  │     └── Compressor (冗余压缩) ──→ 记忆(L2/L3)
  ├── SummarizeStage (环节3: 信息总结)
  │     ├── MemoryRetriever (三源检索 L1/L2/L3)
  │     ├── PromptConstraint (约束注入) ──→ 反幻觉
  │     ├── LLM Summarizer
  │     ├── PostVerify (事后验证) ──→ 反幻觉(事实+来源)
  │     └── MemoryWriter (写入 L2/L3)
  ├── ElevateStage (环节4: 信息升华)
  │     ├── LongTermRetriever (L3 已验证知识检索)
  │     ├── MultiSourceFusion (多源融合)
  │     ├── LogicVerifier (推理链验证) ──→ 反幻觉
  │     ├── CrossValidator (交叉验证) ──→ 反幻觉
  │     └── InsightWriter (洞察写入 L3)
  │
  MemorySystem (记忆系统 — 全流程各环节共享调用)
  │     ├── WorkingMemory (L1, 会话级, 内存)
  │     ├── ShortTermMemory (L2, 可配置寿命, 向量+时间索引)
  │     ├── LongTermMemory (L3, 永久, 语义索引)
  │     ├── Summarizer (会话/日/周/月总结)
  │     ├── Forgetter (时效性遗忘 + 置信度遗忘 + 冗余压缩)
  │     ├── Compressor (记忆压缩: L2→L3)
  │     └── Retriever (语义/关键词/时间/置信度多源检索)
  │
  HallucinationPipeline (反幻觉系统 — 全流程各环节共享调用)
        ├── SourceVerifier (来源验证, 采集/总结时)
        ├── FactScreener (事实初筛, 采集时)
        ├── ConsistencyFilter (一致性过滤, 降噪时)
        ├── PromptConstraint (约束注入, 总结时)
        ├── PostVerify (总结后验证, 总结时)
        ├── LogicVerifier (推理链验证, 升华时)
        ├── CrossValidator (交叉验证, 升华时)
        ├── ConfidenceScorer (综合评分, 各环节输出点)
        ├── HallucinationDB (全局幻觉积累)
        ├── PatternDetector (模式识别)
        ├── PromptOptimizer (自动优化Prompt)
        └── FeedbackLoop (用户纠正→传播)
```

***

## 6. 功能优先级列表

| 优先级    | 功能 ID | 功能名称                  | 估时  | 说明                                               |
| ------ | ----- | --------------------- | --- | ------------------------------------------------ |
| **P0** | F001  | 事件驱动回测引擎              | 3 周 | 核心引擎，零妥协                                         |
| **P0** | F002  | 订单管理系统 OMS            | 3 周 | 5 种订单类型 + 状态机                                    |
| **P0** | F003  | 投资组合模拟                | 2 周 | 多资产多空跟踪                                          |
| **P0** | F004  | 策略框架 BaseStrategy     | 3 周 | 生命周期钩子 + 指标代理                                    |
| **P0** | F005  | 回测统计指标 30+            | 2 周 | Sharpe/Sortino/Calmar 等                          |
| **P0** | F006  | Broker 抽象层            | 2 周 | 回测/模拟/实盘可互换                                      |
| **P0** | F007  | 佣金与滑点建模               | 1 周 | A 股真实费用                                          |
| **P1** | F008  | 参数优化器                 | 2 周 | 网格 + 随机 + Walk-Forward                           |
| **P1** | F009  | 风险管理模块                | 2 周 | 风控规则拦截                                           |
| **P1** | F010  | 仓位管理模块                | 1 周 | Kelly/ATR/波动率                                    |
| **P1** | F011  | 数据层抽象 + 缓存            | 2 周 | DataFeed ABC + 本地缓存                              |
| **P1** | F012  | 模拟盘模式                 | 1 周 | 实时行情验证                                           |
| **P1** | F013  | 回测报表系统                | 2 周 | HTML/JSON 报表                                     |
| **P1** | F018  | 消息推送增强                | 1 周 | 企微/Telegram + AI 推送                              |
| **P0** | F022  | AI 策略生成与配置 Agent      | 3 周 | 自然语言→策略代码 + 策略评分 + 知识库                           |
| **P1** | F021  | AI 指标发现 Agent         | 2 周 | 自动推荐指标+参数                                        |
| **P1** | F023  | AI 回测解读 Agent         | 2 周 | 自然语言解读+过拟合检测                                     |
| **P1** | F024  | AI 实时盯盘 Agent         | 2 周 | 行情+消息联动+异动检测                                     |
| **P1** | F026  | AI 动态风控 Agent         | 2 周 | 市场环境自适应风控                                        |
| **P2** | F014  | 内置策略模板 7 套            | 2 周 | 开箱即用                                             |
| **P1** | F015  | 自定义指标 DSL             | 1 周 | @indicator 装饰器（与 F004 同步开发）                      |
| **P2** | F016  | Web Dashboard         | 2 周 | Streamlit                                        |
| **P2** | F017  | 券商 API 实盘             | 3 周 | XTP/CTP 集成                                       |
| **P2** | F027  | AI 策略对比 Agent         | 1 周 | 多策略横向对比+组合优化                                     |
| **P2** | F028  | AI 自然语言交互界面           | 2 周 | 对话式策略/数据/盯盘                                      |
| **P0** | F019  | 信号管线系统                | 2 周 | AI 信号↔策略信号转换 + A股规则校验                            |
| **P0** | F020  | AI 信息处理全流程            | 7 周 | 采集→降噪→总结→升华 + 记忆+反幻觉贯穿（含记忆系统 6 模块 + 反幻觉系统 10 模块） |
| **P1** | F029  | Web Dashboard 前端      | 4 周 | React + Ant Design + ECharts + Monaco            |
| **P1** | F030  | 前后端集成部署               | 2 周 | Docker Compose + Nginx + FastAPI                 |
| —      | —     | 基础设施（测试/CI/Docker/文档） | 2 周 | 贯穿所有阶段                                           |

**总估时**：约 40-52 周（10-13 个月），含基础设施 + AI 模块 + Web 前端

> **工期说明**：F020（AI 信息处理全流程）7 周为最关键路径，包含完整的记忆系统（6 个文件）、反幻觉系统（10 个文件）、NLP 处理链和爬虫系统。F019（信号管线）2 周与 F004（策略框架）3 周部分并行。F029（Web 前端）4 周与后端 API 开发并行。F030（集成部署）2 周在核心功能开发完成后进行。集成调试时间已计入各模块估时的 +20% 缓冲。

***

## 7. 技术栈

### 核心依赖

| 依赖                    | 版本     | 用途                   |
| --------------------- | ------ | -------------------- |
| numpy                 | ≥ 1.24 | 数值计算                 |
| pandas                | ≥ 2.0  | 数据操作                 |
| talib                 | ≥ 0.4  | 技术指标（C 库）            |
| pandas-ta             | ≥ 0.3  | 技术指标（纯 Python 备用）    |
| plotly                | ≥ 5.0  | 交互式可视化               |
| Jinja2                | ≥ 3.0  | HTML 报表模板            |
| requests              | ≥ 2.28 | HTTP 请求              |
| websocket-client      | ≥ 1.5  | WebSocket 实时数据       |
| beautifulsoup4        | ≥ 4.12 | Web 爬取               |
| feedparser            | ≥ 6.0  | RSS 解析               |
| sqlalchemy            | ≥ 2.0  | 本地数据库                |
| pyarrow               | ≥ 12.0 | Parquet 格式           |
| streamlit             | ≥ 1.20 | Web Dashboard        |
| openai                | ≥ 1.0  | OpenAI LLM API       |
| anthropic             | ≥ 0.18 | Anthropic Claude API |
| httpx                 | ≥ 0.25 | 异步 HTTP 客户端（爬虫）      |
| chromadb              | ≥ 0.4  | 向量数据库（记忆嵌入搜索）        |
| sentence-transformers | ≥ 2.2  | 文本嵌入模型（语义搜索）         |
| pytest                | ≥ 7.0  | 测试框架                 |
| coverage              | ≥ 7.0  | 测试覆盖率                |
| ruff                  | ≥ 0.1  | 代码格式化 + lint         |
| APScheduler           | ≥ 3.10 | 定时任务调度（数据采集）         |

### 前端依赖（Web Dashboard）

| 依赖                | 版本     | 用途                 |
| ----------------- | ------ | ------------------ |
| react             | ≥ 18.2 | UI 框架              |
| react-router-dom  | ≥ 6.0  | 路由                 |
| ant-design        | ≥ 5.0  | 组件库                |
| echarts-for-react | ≥ 3.0  | ECharts 图表         |
| zustand           | ≥ 4.0  | 状态管理               |
| axios             | ≥ 1.0  | HTTP 客户端           |
| monaco-editor     | ≥ 0.40 | 代码编辑器              |
| vite              | ≥ 5.0  | 构建工具               |
| typescript        | ≥ 5.0  | 类型系统               |
| sass              | ≥ 1.0  | CSS 预处理器           |
| dayjs             | ≥ 1.0  | 日期处理               |
| marked            | ≥ 9.0  | Markdown 渲染（AI 对话） |

### 后端依赖（API 网关）

| 依赖              | 版本      | 用途             |
| --------------- | ------- | -------------- |
| fastapi         | ≥ 0.100 | RESTful API 框架 |
| uvicorn         | ≥ 0.20  | ASGI 服务器       |
| pydantic        | ≥ 2.0   | 数据验证           |
| python-jose     | ≥ 0.20  | JWT 认证         |
| python-socketio | ≥ 5.0   | WebSocket 实时推送 |
| aiohttp         | ≥ 3.0   | 异步 HTTP 客户端    |
| gunicorn        | ≥ 21.0  | 生产级 WSGI 进程管理  |
| slowapi         | ≥ 0.1   | API 限流         |

### 可选依赖

| 依赖            | 用途                    |
| ------------- | --------------------- |
| xtp-ctp       | 中泰 XTP / CTP 券商 API   |
| weasyprint    | PDF 报表生成              |
| cryptography  | 配置加密                  |
| ollama-python | 本地 LLM 推理             |
| transformers  | HuggingFace 本地 NLP 模型 |
| duckdb        | 轻量级本地分析数据库            |

***

## 8. API 设计预览

### 策略编写（新版）

```python
from stockquant.strategy import BaseStrategy
from stockquant.indicators import EMA

class MyStrategy(BaseStrategy):
    name = "Dual MA Crossover"
    parameters = {
        "fast_period": 5,
        "slow_period": 20,
        "position_size": 0.1,  # 每次投入 10% 资金
    }

    def on_start(self):
        self.ma_fast = EMA(self.data, period=self.parameters["fast_period"])
        self.ma_slow = EMA(self.data, period=self.parameters["slow_period"])
        self.sizer = FixedFractionSizer(self.parameters["position_size"])

    def on_bar(self):
        if self.ma_fast[0] > self.ma_slow[0] and self.ma_fast[-1] <= self.ma_slow[-1]:
            # 金叉：买入
            qty = self.sizer.calculate(self.portfolio.cash * 0.95 / self.data.close[0])
            self.order_limit(self.data.close[0] * 1.001, qty)
        elif self.ma_fast[0] < self.ma_slow[0] and self.ma_fast[-1] >= self.ma_slow[-1]:
            # 死叉：卖出
            self.close_all()

    def on_trade(self, trade):
        self.log(f"成交: {trade.symbol} {trade.side} {trade.quantity} @ {trade.price:.2f}")
```

### 回测运行

```python
from stockquant.engine import Cerebro
from stockquant.data import DataFeed
from stockquant.broker import BacktestBroker
from stockquant.commission import AShareCommission
from stockquant.slippage import PercentSlippage

cerebro = Cerebro(
    cash=1_000_000,
    broker=BacktestBroker(slippage=PercentSlippage(0.001)),
    commission=AShareCommission(),
    risk_manager=RiskManager(
        max_position_pct=0.3,
        max_daily_loss_pct=0.02,
        max_drawdown_pct=0.15,
    ),
)

cerebro.add_data(DataFeed.baostock(
    symbols=["sh600519", "sz000858"],
    timeframe="1d",
    start="2020-01-01",
    end="2024-12-31",
))

cerebro.add_strategy(MyStrategy, fast_period=5, slow_period=20)

# 参数优化
results = cerebro.optstrategy(
    MyStrategy,
    fast_period=range(3, 10),
    slow_period=range(15, 30),
    optimizer="grid",
)

# 查看报告
cerebro.show_report(results)
```

### AI 对话式策略开发

```python
from stockquant.ai import AIOrchestrator

ai = AIOrchestrator(
    llm_provider="openai",
    llm_model="gpt-4o",
    data_sources=["baostock", "eastmoney", "xueqiu"],
)

# 自然语言生成策略
strategy = ai.generate_strategy(
    "在日线级别，当 MACD 金叉且东方财富论坛情绪超过 70% 时买入，"
    "仓位不超过总资产的 20%，止损 5%"
)

# AI 自动回测
results = cerebro.backtest(strategy)

# AI 解读
analysis = ai.analyze_backtest(results)
print(analysis.summary)          # 自然语言总结
print(analysis.issues)           # 问题列表
print(analysis.suggestions)      # 改进建议

# AI 盯盘
ai.start_monitor(
    symbols=["sh600519", "sz000858"],
    notifier="dingtalk",
    mode="advisory",  # advisory / semi-auto / auto
)
```

### AI 配置

```yaml
# stockquant_config.yaml
ai:
  llm:
    provider: "openai"           # openai / anthropic / local
    model: "gpt-4o"
    api_key: "${AI_API_KEY}"     # 从环境变量读取
    temperature: 0.3
    max_tokens: 2048

  data_collection:
    enabled: true
    frequency: "5min"            # 每 5 分钟采集一次
    sources:
      - name: "eastmoney_news"
        type: "web_scraper"
        schedule: "*/5 * * * *"
      - name: "xueqiu_sentiment"
        type: "web_scraper"
        schedule: "*/10 * * * *"
      - name: "cls_news"
        type: "rss"
        url: "https://www.cls.cn/nodeapi/updateTelegraphList"
      - name: "cninfo_announcement"
        type: "web_scraper"
        schedule: "*/30 * * * *"

  nlp:
    sentiment_model: "clue/albert-base-chinese-sst"  # HuggingFace 模型
    fallback_provider: "openai"   # 本地模型失败时用 LLM 替代

  monitoring:
    enabled: true
    symbols: []                   # 为空时监控全市场
    alert_threshold: 0.7          # 信号融合得分阈值
    notification_channels:
      - "dingtalk"
      - "email"

  decision_mode: "advisory"       # advisory / semi-auto / auto
  risk_override_enabled: false    # AI 是否可以覆盖风控参数
```

***

### Web API 调用示例

```typescript
// 前端：提交回测任务
import { backtestApi } from './api/backtest';

const result = await backtestApi.create({
  strategy_id: 'ma_crossover_v1',
  data_sources: ['baostock'],
  symbols: ['sh600519', 'sz000858'],
  start_date: '2020-01-01',
  end_date: '2024-12-31',
  cash: 1_000_000,
  commission: { type: 'ashare' },
  slippage: { type: 'percent', value: 0.001 },
  risk_rules: { max_position_pct: 0.3, max_daily_loss_pct: 0.02 },
});
// { task_id: "abc123", status: "queued" }

// 前端：WebSocket 接收回测进度
import { useWebSocket } from './hooks/useWebSocket';

const ws = useWebSocket(`ws://${import.meta.env.VITE_API_HOST}/ws/backtest/abc123`);
ws.onMessage((msg) => {
  if (msg.type === 'progress') {
    setProgress(msg.data.progress); // 0-100
  } else if (msg.type === 'metrics') {
    setMetrics(msg.data); // { sharpe: 1.2, max_drawdown: -0.15, ... }
  }
});

// 前端：AI 对话（SSE 流式）
import { aiApi } from './api/ai';

const response = await aiApi.chat({
  message: '帮我分析一下 sh600519 最近的技术面',
  mode: 'advisory',
});
// 流式读取 SSE response → ChatPanel 逐字渲染
```

***

## 9. 向后兼容策略

### 9.1 Breaking Change 处理

由于架构完全重写，v1 API 无法兼容。采用以下策略：

- **StockQuant v1.x** 标记为 EOL（End of Life），不再更新
- **StockQuant v2.0** 全新 API，与 v1 不兼容
- 提供 **Migration Guide** 文档，说明 v1 → v2 的迁移方法
- 提供 **v1\_compat** 模块：包装 v1 风格策略，使其可在 v2 引擎中运行（有限支持，仅单标的日线回测）

### 9.2 迁移指南核心要点

```python
# v1 写法（不再推荐）
from stockquant.quant import *
config.loads('config.json')

class Strategy:
    def __init__(self):
        self.trade = Trade(config_file="config.json", symbol="sh512980")
        kline = Market.kline("sh512980", "1d")
        bt = BackTest()
        for i in range(10, len(kline)):
            bt.initialize(kline[:i+1])
            # ...手动逻辑...

# v2 写法（推荐）
from stockquant.strategy import BaseStrategy
from stockquant.engine import Cerebro

class MyStrategy(BaseStrategy):
    def on_bar(self):
        if self.is_last_bar():
            pass  # 不再需要手动迭代

cerebro = Cerebro()
cerebro.add_data(DataFeed.baostock("sh512980", "1d"))
cerebro.add_strategy(MyStrategy)
cerebro.run()
```

***

## 10. 测试策略

### 10.1 测试覆盖目标

| 模块                  | 目标覆盖率 | 测试类型                    |
| ------------------- | ----- | ----------------------- |
| engine/cerebro.py   | 100%  | 单元测试 + 集成测试             |
| engine/event.py     | 100%  | 单元测试                    |
| engine/oms.py       | 100%  | 单元测试（订单状态机）             |
| engine/portfolio.py | 100%  | 单元测试（资金计算）              |
| engine/broker/\*.py | 100%  | 单元测试 + 集成测试             |
| indicators/\*.py    | 100%  | 单元测试（与 TA-Lib 独立计算对比）   |
| strategy/base.py    | 100%  | 单元测试 + 集成测试             |
| analytics/          | 95%   | 单元测试                    |
| data/               | 95%   | 单元测试 + 集成测试             |
| execution/          | 90%   | 单元测试                    |
| api/                | 95%   | 单元测试 + 集成测试             |
| web/                | 85%   | 单元测试（组件） + E2E（Cypress） |

### 10.2 测试策略

- **单元测试**：每个函数独立测试，Mock 外部依赖
- **集成测试**：完整回测流程端到端测试
- **回归测试**：用已知结果的策略（如 MA 交叉）验证引擎正确性
- **性能测试**：回测速度基准测试，确保新版本不低于旧版本
- **数据验证测试**：指标计算结果与 TA-Lib 独立计算结果对比

### 前端测试策略

- **单元测试**：Jest + React Testing Library，每个组件独立测试
- **E2E 测试**：Cypress（回测流程、AI 对话、盯盘推送全流程）
- **API 测试**：pytest + httpx（FastAPI TestClient），测试所有 REST + WebSocket 端点
- **Snapshot 测试**：关键组件（Chart/Table/AI 卡片）UI 不变性验证

***

## 11. 发布计划

| 版本           | 时间     | 内容                                                                       |
| ------------ | ------ | ------------------------------------------------------------------------ |
| v2.0.0-alpha | 第 10 周 | 事件引擎 + OMS + 投资组合 + 策略基类 + 18 指标 + AI 数据采集 Agent                         |
| v2.0.0-beta  | 第 18 周 | + 佣金/滑点 + 参数优化 + 风控 + 报表 + Broker 抽象 + AI 策略生成 + AI 辅助决策 + API 网关        |
| v2.0.0-rc    | 第 24 周 | + 数据缓存 + 模拟盘 + AI 回测解读 + AI 盯盘 + 动态风控 + Web Dashboard Alpha + 测试覆盖 ≥ 90% |
| v2.0.0       | 第 36 周 | + Web Dashboard 完成版 + AI 对话界面 + 券商 API + Docker Compose 一键部署             |
| v2.1.0       | 第 48 周 | Web Dashboard 高级功能（多用户、Streamlit 高级分析、移动端适配）                             |

***

## 12. 风险与缓解

| 风险                  | 影响              | 缓解措施                                   |
| ------------------- | --------------- | -------------------------------------- |
| TA-Lib C 库安装失败      | 安装困难            | 提供 pandas-ta 纯 Python 替代               |
| BaoStock API 变动     | 历史数据中断          | 本地缓存 + Tushare 备用                      |
| 网易 API 变动           | 实时数据中断          | 引入 Tushare Pro 实时行情                    |
| XTP API 集成复杂        | 实盘延迟            | 先支持回测/模拟盘，实盘逐步推进                       |
| 项目周期过长              | 人员变动风险          | 分版本发布，每个 Alpha 可用                      |
| 文档不足                | 用户学习成本高         | 边开发边写文档，每个 Alpha 配套教程                  |
| **LLM API 不可用/成本高** | AI 功能失效/成本超支    | 本地模型降级 + 每日成本预算限制 + 规则引擎兜底             |
| **爬虫被反爬**           | 数据采集中断          | 多源冗余 + 代理池 + 速率限制 + 失败告警               |
| **AI 幻觉导致错误交易**     | 资金损失            | 全自动模式需人工确认 + AI 输出必须引用来源 + 风控层二次验证     |
| **AI 决策不透明**        | 合规风险            | 所有 AI 调用记录到审计日志 + Chain-of-Thought 可追溯 |
| **LLM 供应商锁定**       | 迁移困难            | LLM 适配层抽象 + 多供应商支持（OpenAI/Claude/本地）   |
| **前端构建依赖冲突**        | 前端 npm 依赖版本冲突   | npm lockfile + 固定依赖版本 + 独立 CI 环境       |
| **CORS 跨域问题**       | 前后端分离部署时的跨域     | Nginx 反向代理统一域名                         |
| **WebSocket 连接不稳定** | 网络波动导致断连        | 自动重连 + 指数退避 + 心跳检测                     |
| **大表格渲染卡顿**         | 10 万笔交易明细导致前端卡顿 | 虚拟滚动 + 分页加载 + 服务端分页                    |

***

## 13. 成功指标

| 指标                  | 目标值                    |
| ------------------- | ---------------------- |
| 测试覆盖率               | ≥ 90%                  |
| 回测速度与 backtrader 持平 | ± 20% 以内               |
| 回测指标与 vectorbt 结果一致 | ± 0.1% 以内              |
| 新用户 30 分钟完成第一个回测    | 通过用户测试验证               |
| PyPI 下载量（发布 6 个月内）  | ≥ 5000                 |
| GitHub Star         | ≥ 2000                 |
| 内置策略模板              | ≥ 7 套                  |
| 支持数据源               | ≥ 5 种                  |
| AI 情感分析准确率          | ≥ 75%                  |
| 数据采集源数量             | ≥ 20 个                 |
| LLM 供应商支持           | ≥ 2 种（OpenAI + Claude） |
| AI 决策可追溯率           | 100%（所有调用记录审计日志）       |
| 记忆检索响应时间            | < 200ms                |
| 信息压缩核心事实保留率         | ≥ 95%                  |
| 幻觉纠正通过率（严格模式）       | ≥ 99%（通过事实验证的建议）       |
| 幻觉模式识别准确率           | ≥ 80%                  |
| 用户纠正后同类错误再犯率        | ≤ 5%                   |
| Web 首屏加载时间          | < 3 秒（Lighthouse ≥ 80） |
| API 响应延迟（P95）       | < 200ms（常规查询）          |
| WebSocket 实时推送延迟    | < 500ms                |
| Cypress E2E 测试通过率   | 100%（核心流程）             |

