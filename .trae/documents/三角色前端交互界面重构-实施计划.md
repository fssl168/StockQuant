# 三角色前端交互界面重构 - 实施计划

> **阶段**：Phase B2-G（Phase A 和 Phase B1 已完成）
> **当前状态**：Phase A 和 Phase B1 已完成 ✅

---

## 已完成工作回顾

### Phase A：后端鉴权修复与用户管理 ✅
- audit.py/trading.py/optimize.py/data.py 鉴权修复
- 创建 user_admin.py 用户管理路由（8 个端点）
- 注册到 main.py
- 种子用户导入功能
- 直接使用 repository.py 现有函数

### Phase B1：AppLayout 重写 ✅
- 完整重写 AppLayout.tsx
- 实现分组菜单（交易研究/市场数据/AI 助手/系统管理）
- 角色过滤逻辑
- 顶部栏角色徽标（emoji + 颜色）
- 用户菜单（头像 + 用户名 + 退出登录）
- 创建 Forbidden.tsx 403 页面

---

## 待实施工作

### Phase B2: 路由守卫加固

**文件**：[web/src/App.tsx](file:///d:/projects/StockQuant/web/src/App.tsx)

**改动**：
1. 修改 `RoleRoute` 组件，拒绝时跳转到 `/403` 页面而非静默跳转 `/`
2. 为系统管理页面添加 `RoleRoute requiredRoles={['ADMIN']}` 包裹
3. 为 `/trading` 添加 `RoleRoute requiredRoles={['TRADER', 'ADMIN']}`
4. 删除冗余 `/trading-demo` 路由

**具体修改**：

```typescript
// 1. 修改 RoleRoute - 跳转到 403
function RoleRoute({
  children,
  requiredRoles = ['VIEWER', 'TRADER', 'ADMIN']
}: {
  children: React.ReactNode
  requiredRoles?: string[]
}) {
  const { user } = useAuthStore()
  // ... 现有角色检查逻辑不变 ...
  if (!hasPermission(requiredRoles, userRoles)) {
    return <Navigate to="/403" replace state={{ required: requiredRoles }} />
  }
  return <>{children}</>
}

// 2. 导入 Forbidden 页面
import Forbidden from './pages/Forbidden'

// 3. 新增 403 路由
<Route path="/403" element={<Forbidden />} />

// 4. 为系统管理页面添加角色守卫
<Route path="/settings" element={<RoleRoute requiredRoles={['ADMIN']}><Settings /></RoleRoute>} />
<Route path="/memory" element={<RoleRoute requiredRoles={['ADMIN']}><Memory /></RoleRoute>} />
<Route path="/hallucination" element={<RoleRoute requiredRoles={['ADMIN']}><Hallucination /></RoleRoute>} />
<Route path="/ai-pipeline" element={<RoleRoute requiredRoles={['ADMIN']}><AIPipeline /></RoleRoute>} />

// 5. 为 trading 添加角色守卫
<Route path="/trading" element={<RoleRoute requiredRoles={['TRADER', 'ADMIN']}><Trading /></RoleRoute>} />

// 6. 删除 /trading-demo 路由
// <Route path="/trading-demo" element={<TradingDemo />} />  // 删除
```

---

### Phase C: 创建新增页面

#### C1. 用户管理页面

**新建**：`web/src/pages/admin/UserManagement.tsx`

**功能**：
- 表格展示所有用户（用户名、角色、状态、创建时间）
- 操作列：编辑角色、重置密码、启用/禁用
- 顶部"新建用户"按钮
- 调用 `/api/admin/users` 系列端点
- 使用 Zustand store 管理数据

**新建**：`web/src/stores/userStore.ts`

```typescript
import { create } from 'zustand'
import client from '@/api/client'

interface User {
  id: string
  username: string
  roles: string[]
  disabled: boolean
  createdAt: string
  lastLoginAt?: string
}

interface UserState {
  users: User[]
  loading: boolean
  fetchUsers: () => Promise<void>
  createUser: (data: { username: string; password: string; roles: string[] }) => Promise<void>
  updateUser: (userId: string, data: { roles?: string[]; disabled?: boolean }) => Promise<void>
  resetPassword: (userId: string, password: string) => Promise<void>
  toggleDisable: (userId: string) => Promise<void>
  deleteUser: (userId: string) => Promise<void>
}

export const useUserStore = create<UserState>((set) => ({
  users: [],
  loading: false,

  fetchUsers: async () => {
    set({ loading: true })
    try {
      const res = await client.get('/api/admin/users')
      set({ users: (res as User[]).map(normalizeUser), loading: false })
    } catch (e: any) {
      set({ loading: false })
      throw e
    }
  },

  createUser: async (data) => {
    await client.post('/api/admin/users', data)
    // refetch
    const { fetchUsers } = useUserStore.getState()
    await fetchUsers()
  },

  updateUser: async (userId, data) => {
    await client.put(`/api/admin/users/${userId}`, data)
    const { fetchUsers } = useUserStore.getState()
    await fetchUsers()
  },

  resetPassword: async (userId, password) => {
    await client.post(`/api/admin/users/${userId}/password`, { password })
  },

  toggleDisable: async (userId) => {
    await client.post(`/api/admin/users/${userId}/toggle-disable`)
    const { fetchUsers } = useUserStore.getState()
    await fetchUsers()
  },

  deleteUser: async (userId) => {
    await client.delete(`/api/admin/users/${userId}`)
    const { fetchUsers } = useUserStore.getState()
    await fetchUsers()
  },
}))

function normalizeUser(u: any): User {
  return {
    id: u.id ?? u.userId ?? '',
    username: u.username ?? '',
    roles: Array.isArray(u.roles) ? u.roles : [u.roles ?? 'VIEWER'],
    disabled: !!u.disabled,
    createdAt: u.createdAt ?? u.created_at ?? '',
    lastLoginAt: u.lastLoginAt ?? u.last_login_at,
  }
}
```

**UserManagement.tsx 结构**：
- 顶部：标题 + "新建用户"按钮
- 表格：用户名 / 角色（Tag）/ 状态（Switch）/ 创建时间 / 操作列
- 操作列：编辑角色（Modal 下拉多选）、重置密码（Modal 输入）、禁用/启用（Switch）、删除（Popconfirm）
- 新建用户 Modal：用户名、密码、角色下拉

---

#### C2. 调度器管理页面

**新建**：`web/src/pages/admin/SchedulerAdmin.tsx`

**功能**：
- 双调度器面板：通用调度器 + AI 管线调度器
- 每个面板：状态卡片（运行中/停止）+ 启动/停止按钮 + 任务列表
- 任务列表：新增任务、删除任务、上次/下次执行时间

**后端端点参考**：
- `/api/scheduler/*`
- `/api/pipeline/scheduler/*`

---

#### C3. 审计日志页面

**新建**：`web/src/pages/admin/AuditLog.tsx`

**功能**：
- 顶部切换：我的日志 / 全部日志
- 筛选器：用户名、信号源、标的、时间范围、最终动作
- 表格：时间、用户、信号源、标的、方向、AI 决策摘要、最终动作
- 详情 Modal

**后端端点参考**：
- `/api/audit/logs`（用户自己的）
- `/api/audit/logs/all`（全部，仅 ADMIN）

---

### Phase B3: 创建权限工具

#### B3a. usePermission Hook

**新建**：`web/src/hooks/usePermission.ts`

```typescript
import { useAuthStore } from '@/stores/authStore'

function getUserRoles(user: { roles?: string[]; role?: string } | null): string[] {
  if (!user) return []
  const roles: string[] = []
  if (Array.isArray(user.roles)) {
    roles.push(...user.roles.map(r => r.toUpperCase()))
  }
  if (typeof user.role === 'string' && user.role) {
    roles.push(user.role.toUpperCase())
  }
  return roles
}

export function usePermission() {
  const { user } = useAuthStore()
  const roles = getUserRoles(user)
  return {
    isAdmin: roles.includes('ADMIN'),
    isTrader: roles.includes('TRADER') || roles.includes('ADMIN'),
    isViewer: roles.includes('VIEWER') || roles.length === 0,
    can: (requiredRoles: string[]) => requiredRoles.some(r => roles.includes(r)),
    canWrite: roles.includes('TRADER') || roles.includes('ADMIN'),
    canManage: roles.includes('ADMIN'),
  }
}
```

#### B3b. PermissionButton 组件

**新建**：`web/src/components/PermissionButton.tsx`

```typescript
import { Button, Tooltip, type ButtonProps } from 'antd'
import { usePermission } from '@/hooks/usePermission'

interface PermissionButtonProps extends ButtonProps {
  requiredRoles: string[]
  mode?: 'disable' | 'hide'
}

export function PermissionButton({ requiredRoles, mode = 'disable', children, ...props }: PermissionButtonProps) {
  const { can } = usePermission()
  if (!can(requiredRoles)) {
    if (mode === 'hide') return null
    return <Tooltip title="无权限"><Button {...props} disabled>{children}</Button></Tooltip>
  }
  return <Button {...props}>{children}</Button>
}
```

#### B3c. RoleBadge 组件

**新建**：`web/src/components/RoleBadge.tsx`

```typescript
import { Badge } from 'antd'

const ROLE_META: Record<string, { label: string; color: string }> = {
  ADMIN: { label: '管理员', color: '#ef4444' },
  TRADER: { label: '交易员', color: '#3b82f6' },
  VIEWER: { label: '观察者', color: '#6b7280' },
}

interface RoleBadgeProps {
  role: string
  size?: 'small' | 'default'
}

export function RoleBadge({ role, size = 'default' }: RoleBadgeProps) {
  const meta = ROLE_META[role?.toUpperCase()] || ROLE_META.VIEWER
  return (
    <Badge
      count={meta.label}
      style={{
        backgroundColor: `${meta.color}22`,
        borderColor: meta.color,
        borderWidth: 1,
        borderStyle: 'solid',
        borderRadius: 4,
        padding: size === 'small' ? '0 6px' : '2px 8px',
      }}
    />
  )
}
```

---

### Phase D: 现有页面权限细化

#### D1. Strategy.tsx

**文件**：[web/src/pages/Strategy.tsx](file:///d:/projects/StockQuant/web/src/pages/Strategy.tsx)

**改动**：
1. 导入 `usePermission`
2. "新建策略"按钮：仅 TRADER/ADMIN 可见 → 用 `PermissionButton` 包裹
3. "AI 生成策略"按钮：仅 TRADER/ADMIN 可见
4. 列表行操作列：VIEWER 只显示"查看"，隐藏删除按钮
5. Monaco 编辑器保存按钮：VIEWER disabled

```typescript
// 新增导入
import { usePermission, PermissionButton } from '@/hooks/usePermission'

// 在组件内
const { canWrite } = usePermission()

// 按钮级权限
<PermissionButton requiredRoles={['TRADER', 'ADMIN']} icon={<Plus size={16} />} onClick={handleCreate}>
  新建策略
</PermissionButton>
<PermissionButton requiredRoles={['TRADER', 'ADMIN']} type="primary" icon={<Sparkle size={16} />} onClick={() => setAiModal(true)}>
  AI 生成策略
</PermissionButton>

// 列表操作列
render: {
  // VIEWER 看不到删除按钮
  canWrite ? (
    <>
      <Button onClick={() => handleEdit(r)} />
      <Popconfirm onConfirm={() => deleteStrategy(r.id)}>
        <Button danger />
      </Popconfirm>
    </>
  ) : (
    <Button onClick={() => handleEdit(r)} />
  )
}
```

---

#### D2. Backtest.tsx

**文件**：[web/src/pages/Backtest.tsx](file:///d:/projects/StockQuant/web/src/pages/Backtest.tsx)

**改动**：
- 全部角色均可提交回测
- 仅 TRADER/ADMIN 可删除回测记录
- 在历史回测列表的每个条目中添加删除按钮

（Backtest 页面当前无删除功能，暂不改动。回测结果页面 BacktestResult.tsx 若有删除按钮则需加权限。）

---

#### D3. Data.tsx

**文件**：[web/src/pages/Data.tsx](file:///d:/projects/StockQuant/web/src/pages/Data.tsx)

**改动**：
1. "清除缓存"按钮：仅 ADMIN 可见
2. "采集"按钮：仅 ADMIN 可见
3. "下载"按钮：VIEWER/TRADER 可见

```typescript
// 新增导入
import { usePermission, PermissionButton } from '@/hooks/usePermission'

// 在组件内
const { canManage } = usePermission()

// 清除缓存按钮
{canManage && (
  <Button size="small" danger onClick={handleClearCache} loading={clearingCache}>
    清除缓存
  </Button>
)}

// 采集按钮 - 在 dataSourceColumns 的 render 中
render: {
  <Space>
    <Button onClick={() => handleDownload(r.provider)}>下载</Button>
    {canManage && (
      <Button onClick={() => { setCollectTargetProvider(r.provider); setCollectModalOpen(true) }}>
        采集
      </Button>
    )}
  </Space>
}
```

---

### Phase E: API 客户端同步

**新建**：`web/src/api/admin.ts`

```typescript
import client from './client'

export const adminApi = {
  listUsers: () => client.get('/api/admin/users'),
  createUser: (data: { username: string; password: string; roles: string[] }) =>
    client.post('/api/admin/users', data),
  updateUser: (userId: string, data: { roles?: string[]; disabled?: boolean }) =>
    client.put(`/api/admin/users/${userId}`, data),
  resetPassword: (userId: string, password: string) =>
    client.post(`/api/admin/users/${userId}/password`, { password }),
  toggleDisable: (userId: string) =>
    client.post(`/api/admin/users/${userId}/toggle-disable`),
  deleteUser: (userId: string) =>
    client.delete(`/api/admin/users/${userId}`),
}
```

---

### Phase F: 登录页增强

**文件**：[web/src/pages/Login.tsx](file:///d:/projects/StockQuant/web/src/pages/Login.tsx)

**改动**：在登录表单下方添加演示账号快捷登录区域

```typescript
// 新增演示账号快捷登录
<div style={{ marginTop: 16 }}>
  <Text style={{ color: '#71717a', fontSize: 12, display: 'block', marginBottom: 8 }}>演示账号快捷登录</Text>
  <Space direction="vertical" style={{ width: '100%' }} size={6}>
    <Button
      size="small"
      onClick={() => { form.setFieldsValue({ username: 'admin', password: 'admin123' }); handleSubmit({ username: 'admin', password: 'admin123' }) }}
      style={{ width: '100%', justifyContent: 'flex-start', fontSize: 11 }}
    >
      管理员 admin / admin123
    </Button>
    <Button
      size="small"
      onClick={() => { form.setFieldsValue({ username: 'trader', password: 'trader123' }); handleSubmit({ username: 'trader', password: 'trader123' }) }}
      style={{ width: '100%', justifyContent: 'flex-start', fontSize: 11 }}
    >
      交易员 trader / trader123
    </Button>
    <Button
      size="small"
      onClick={() => { form.setFieldsValue({ username: 'viewer', password: 'viewer123' }); handleSubmit({ username: 'viewer', password: 'viewer123' }) }}
      style={{ width: '100%', justifyContent: 'flex-start', fontSize: 11 }}
    >
      观察者 viewer / viewer123
    </Button>
  </Space>
</div>
```

---

## 文件改动汇总

### 新建文件（6 个）
| 文件 | 用途 | Phase |
|------|------|-------|
| `web/src/pages/admin/UserManagement.tsx` | 用户管理页面 | C1 |
| `web/src/stores/userStore.ts` | 用户管理 Store | C1 |
| `web/src/pages/admin/SchedulerAdmin.tsx` | 调度器管理页面 | C2 |
| `web/src/pages/admin/AuditLog.tsx` | 审计日志页面 | C3 |
| `web/src/hooks/usePermission.ts` | 权限 Hook | B3a |
| `web/src/components/PermissionButton.tsx` | 权限按钮组件 | B3b |
| `web/src/components/RoleBadge.tsx` | 角色徽标组件 | B3c |
| `web/src/api/admin.ts` | 用户管理 API 客户端 | E |

### 修改文件（5 个）
| 文件 | 改动 | Phase |
|------|------|-------|
| `web/src/App.tsx` | 路由守卫加固 + 删除 trading-demo 路由 | B2 |
| `web/src/pages/Strategy.tsx` | 按钮级权限 | D1 |
| `web/src/pages/Data.tsx` | 按钮级权限 | D3 |
| `web/src/pages/Login.tsx` | 演示账号快捷登录 | F |

---

## 实施顺序

1. **Phase B2** - 路由守卫加固（改动小，影响大）
2. **Phase B3** - 权限工具（其他 Phase 依赖）
3. **Phase C** - 新增页面（C1 用户管理最重要）
4. **Phase D** - 现有页面权限细化（D1 Strategy, D3 Data）
5. **Phase E** - API 客户端（C1 依赖）
6. **Phase F** - 登录页增强（独立改动）
